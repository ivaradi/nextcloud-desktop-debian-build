diff --git a/CMakeLists.txt b/CMakeLists.txt
index 30f1d50973..266f0cfb34 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -30,7 +30,7 @@ set(BIN_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
 include(${CMAKE_SOURCE_DIR}/NEXTCLOUD.cmake)
 
 set(QT_VERSION_MAJOR "6")
-set(REQUIRED_QT_VERSION "6.10.0")
+set(REQUIRED_QT_VERSION "6.2.4")
 
 # CfAPI Shell Extensions
 set( CFAPI_SHELL_EXTENSIONS_LIB_NAME CfApiShellExtensions )
@@ -81,13 +81,11 @@ endif()
 
 set(APPLE_SUPPRESS_X11_WARNING ON)
 
-find_package(ECM 6.0.0 REQUIRED NO_MODULE)
+find_package(ECM 5.0.0 REQUIRED NO_MODULE)
 set_package_properties(ECM PROPERTIES TYPE REQUIRED DESCRIPTION "Extra CMake Modules." URL "https://invent.kde.org/frameworks/extra-cmake-modules")
 
 set(CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake/modules ${ECM_MODULE_PATH} ${CMAKE_MODULE_PATH})
 
-include(KDEInstallDirs)
-include(KDECMakeSettings)
 include(ECMMarkNonGuiExecutable)
 include(ECMSetupVersion)
 include(ECMQmlModule)
@@ -153,7 +151,7 @@ if(APPLE AND BUILD_OWNCLOUD_OSX_BUNDLE)
     add_definitions(-DBUILD_OWNCLOUD_OSX_BUNDLE)
 endif()
 
-find_package(Qt${QT_MAJOR_VERSION} COMPONENTS Core)
+find_package(QT REQUIRED COMPONENTS Core NAMES Qt${QT_MAJOR_VERSION} )
 option(QUICK_COMPILER "Use QtQuick compiler to improve performance" OFF)
 
 # this option removes Http authentication, keychain, shibboleth etc and is intended for
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index e2669c5e56..f7d743c398 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -3,7 +3,7 @@
 # SPDX-License-Identifier: GPL-2.0-or-later
 include(ECMEnableSanitizers)
 
-set(REQUIRED_QT_VERSION "6.5.0")
+set(REQUIRED_QT_VERSION "6.2.4")
 
 find_package(Qt${QT_MAJOR_VERSION}Core ${REQUIRED_QT_VERSION} CONFIG QUIET)
 set_package_properties(Qt${QT_MAJOR_VERSION}Core PROPERTIES
diff --git a/src/cmd/cmd.cpp b/src/cmd/cmd.cpp
index 45d7d1773e..3df1b398a0 100644
--- a/src/cmd/cmd.cpp
+++ b/src/cmd/cmd.cpp
@@ -50,6 +50,8 @@
 
 #include "config.h"
 
+#include "common/qtcompat.h"
+
 #ifdef Q_OS_WIN32
 #include <windows.h>
 #else
diff --git a/src/common/common.cmake b/src/common/common.cmake
index b618bdbead..cc9ca1d956 100644
--- a/src/common/common.cmake
+++ b/src/common/common.cmake
@@ -48,3 +48,5 @@ elseif(UNIX AND NOT APPLE)
 endif()
 
 configure_file(${CMAKE_CURRENT_LIST_DIR}/vfspluginmetadata.json.in ${CMAKE_CURRENT_BINARY_DIR}/vfspluginmetadata.json)
+
+find_package(Qt6 REQUIRED COMPONENTS Widgets)
diff --git a/src/common/path.cpp b/src/common/path.cpp
index 566713b11e..4f8a4971a6 100644
--- a/src/common/path.cpp
+++ b/src/common/path.cpp
@@ -4,6 +4,7 @@
 #include "path.h"
 
 #include "common/filesystembase.h"
+#include "common/qtcompat.h"
 
 OCC::FileSystem::Path::Path(QStringView path)
     : _path(toFilesystemPath(path.toString()))
diff --git a/src/common/qtcompat.cpp b/src/common/qtcompat.cpp
new file mode 100644
index 0000000000..ccded5d62e
--- /dev/null
+++ b/src/common/qtcompat.cpp
@@ -0,0 +1 @@
+#include "qtcompat.h"
diff --git a/src/common/qtcompat.h b/src/common/qtcompat.h
new file mode 100644
index 0000000000..c9284a416a
--- /dev/null
+++ b/src/common/qtcompat.h
@@ -0,0 +1,485 @@
+
+#pragma once
+
+#include <QtGlobal>
+#include <QLocale>
+#include <QRegularExpression>
+#include <QTimeZone>
+#include <QString>
+
+#include <type_traits>
+
+//-----------------------------------------------------------------------------
+
+template<unsigned major, unsigned minor, class T = void>
+struct qt_atleast : public std::enable_if<QT_VERSION>=QT_VERSION_CHECK(major, minor, 0), T>
+{
+};
+
+template<unsigned major, unsigned minor, class T = void>
+struct qt_before : public std::enable_if<QT_VERSION<QT_VERSION_CHECK(major, minor, 0), T>
+{
+};
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+inline QStringList QLocale_uiLanguages()
+{
+#if QT_VERSION>=QT_VERSION_CHECK(6, 8, 0)
+    return QLocale::system().uiLanguages(QLocale::TagSeparator::Underscore);
+#else
+    auto languages = QLocale::system().uiLanguages();
+    for (auto& language: languages) {
+        language.replace(QLatin1Char('-'), QLatin1Char('_'));
+    }
+    return languages;
+#endif
+}
+
+//-----------------------------------------------------------------------------
+
+template <typename T>
+inline QRegularExpressionMatch
+matchRegularExpression(const QRegularExpression& re, const T& s)
+{
+#if QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+    return re.matchView(s);
+#else
+    return re.match(s);
+#endif
+}
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 5, 0)
+template <typename Char, typename... Args>
+inline QDebug& operator<<(QDebug& d, const std::basic_string<Char, Args...>& s)
+{
+    return d << s.c_str();
+}
+#endif
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 6, 0)
+
+#include <chrono>
+
+template <typename Rep, typename Period>
+inline QDebug& operator<<(QDebug& d, const std::chrono::duration<Rep, Period> dur)
+{
+    return d << dur.count() << " " << Period::num << "/" << Period::den;
+}
+#endif
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+const auto QTimeZoneUTC = QTimeZone::UTC;
+#else
+const auto QTimeZoneUTC = Qt::UTC;
+#endif
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 4, 0)
+#define QCOMPARE_EQ(computed, baseline) QCOMPARE(computed==baseline, true)
+#define QCOMPARE_GT(computed, baseline) QCOMPARE(computed>baseline, true)
+#endif
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 4, 0)
+#define QLatin1StringOrView QLatin1String
+#define QLatin1StringViewOrStringHeader <QLatin1String>
+#else
+#define QLatin1StringOrView QLatin1StringView
+#define QLatin1StringViewOrStringHeader <QLatin1StringView>
+#endif
+
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 5, 0)
+#define QmlIntegrationHeader <QtQml/qqmlregistration.h>
+#else
+// QtQmlIntegration lives in its own Qt module (Qt6::QmlIntegration). Using
+// the older header is preferable for compatibility: it works in all Qt 6
+// versions as long as the target links to Qt6::Qml, which it already does.
+#define QmlIntegrationHeader <QtQml/qqmlregistration.h>
+#endif
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 4, 0)
+namespace Qt { namespace StringLiterals {
+
+constexpr inline QLatin1StringOrView operator""_L1(const char *str, size_t size)
+{
+    return QLatin1StringOrView(str, static_cast<qsizetype>(size));
+}
+
+constexpr inline QLatin1Char operator""_L1(char c) noexcept
+{
+    return QLatin1Char(c);
+}
+
+inline QString operator""_s(const char16_t *str, size_t size)
+{
+    return QString::fromUtf16(str, static_cast<qsizetype>(size));
+}
+
+inline QByteArray operator""_ba(const char *str, size_t size)
+{
+    return QByteArray(str, size);
+}
+
+} }
+#endif
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 5, 0)
+#include <QDebug>
+#define qCFatal(category) QDebug(QtFatalMsg)
+
+// Q_UNREACHABLE_RETURN() was introduced in Qt 6.5 (in qassert.h).
+// For older Qt versions, provide an equivalent implementation.
+#define Q_UNREACHABLE_RETURN(...) do { Q_UNREACHABLE(); return __VA_ARGS__; } while (false)
+#endif
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 6, 0)
+#define QPromiseEmplaceResult(promise, type, ...) promise->addResult(type(__VA_ARGS__))
+#else
+#define QPromiseEmplaceResult(promise, type, ...) promise->emplaceResult(__VA_ARGS__)
+#endif
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#ifdef QT_WIDGETS_LIB
+
+//-----------------------------------------------------------------------------
+
+#include <QCheckBox>
+
+//-----------------------------------------------------------------------------
+
+template <typename Functor>
+inline QMetaObject::Connection connectCheckBoxStateChanged(
+    const QCheckBox* sender, const QObject* context, Functor functor)
+{
+    return QObject::connect(sender,
+#if QT_VERSION>=QT_VERSION_CHECK(6, 8, 0)
+                            &QCheckBox::checkStateChanged,
+#else
+                            &QCheckBox::stateChanged,
+#endif
+                            context, functor);
+}
+
+//-----------------------------------------------------------------------------
+
+#endif // QT_WIDGETS_LIB
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#ifdef QT_WEBSOCKETS_LIB
+
+//-----------------------------------------------------------------------------
+
+#include <QWebSocket>
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+#define QWebSocketErrorOccurred &QWebSocket::errorOccurred
+#else
+#define QWebSocketErrorOccurred &QWebSocket::error
+#endif
+
+//-----------------------------------------------------------------------------
+
+#endif // QT_WEBSOCKETS_LIB
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#ifdef QT_XML_LIB
+
+//-----------------------------------------------------------------------------
+
+#include <QDomDocument>
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+
+inline QDomDocument::ParseResult
+QDomDocumentSetContentsUseNamespaceProcessing(QDomDocument& doc, QIODevice* device)
+{
+    return doc.setContent(device, QDomDocument::ParseOption::UseNamespaceProcessing);
+}
+
+#else // QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+
+struct QDomDocumentParseResult {
+    bool retval = false;
+
+    QString errorMessage;
+
+    int errorLine = -1;
+
+    int errorColumn = -1;
+
+    inline operator bool() const {
+        return retval;
+    }
+};
+
+inline QDomDocumentParseResult
+QDomDocumentSetContentsUseNamespaceProcessing(QDomDocument& doc, QIODevice* device)
+{
+    QDomDocumentParseResult result;
+    result.retval = doc.setContent(device, true, &result.errorMessage,
+                                   &result.errorLine, &result.errorColumn);
+    return result;
+}
+
+#endif // QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+
+//-----------------------------------------------------------------------------
+
+#endif // QT_XML_LIB
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#ifdef QT_GUI_LIB
+
+//-----------------------------------------------------------------------------
+
+#include <QGuiApplication>
+#include <QStyleHints>
+
+//-----------------------------------------------------------------------------
+
+inline double getColorDarkness(const QColor &color)
+{
+    // account for different sensitivity of the human eye to certain colors
+    const double threshold = 1.0 - (0.299 * color.red() + 0.587 * color.green() + 0.114 * color.blue()) / 255.0;
+    return threshold;
+}
+
+//-----------------------------------------------------------------------------
+
+inline bool isDarkColor(const QColor& color)
+{
+    return getColorDarkness(color) > 0.5;
+}
+
+//-----------------------------------------------------------------------------
+
+template <class Target, typename Functor>
+inline void connectQStyleHintsColorSchemeChanged(QGuiApplication* app,
+                                                 const Target* context,
+                                                 Functor functor)
+{
+#if QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+    QObject::connect(app->styleHints(), &QStyleHints::colorSchemeChanged,
+                     context, functor, Qt::UniqueConnection);
+#else
+    QObject::connect(app, &QGuiApplication::paletteChanged, context, functor);
+#endif
+}
+
+//-----------------------------------------------------------------------------
+
+inline bool isDarkMode() {
+#if QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+    switch (qGuiApp->styleHints()->colorScheme())
+    {
+      case Qt::ColorScheme::Dark:
+        return true;
+      case Qt::ColorScheme::Light:
+        return false;
+      case Qt::ColorScheme::Unknown:
+        return isDarkColor(QGuiApplication::palette().window().color());
+    }
+
+    return false;
+#else // QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+    return isDarkColor(QGuiApplication::palette().window().color());
+#endif // QT_VERSION>=QT_VERSION_CHECK(6, 5, 0)
+}
+
+//-----------------------------------------------------------------------------
+
+#endif // QT_GUI_LIB
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#ifdef QT_NETWORK_LIB
+
+//-----------------------------------------------------------------------------
+
+#include <QNetworkRequest>
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 8, 0)
+inline void removeAuthorizationHeaders(QNetworkRequest& request)
+{
+    request.setRawHeader("Authorization", QByteArray());
+}
+#else
+inline void removeAuthorizationHeaders(QNetworkRequest& request)
+{
+    auto headers = request.headers();
+    headers.removeAll(QHttpHeaders::WellKnownHeader::Authorization);
+    request.setHeaders(headers);
+}
+#endif
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 3, 0)
+#include <QNetworkReply>
+// QNetworkReply::requestSent() signal was introduced in Qt 6.3
+// Use finished() as a fallback - it fires when the request is sent on older Qt versions
+#define QNetworkReplyRequestSent &QNetworkReply::finished
+#else
+#include <QNetworkReply>
+#define QNetworkReplyRequestSent &QNetworkReply::requestSent
+#endif
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 7, 0)
+#include <QNetworkReply>
+inline bool QNetworkReplyHasRawHeader(QNetworkReply *reply, const char *header)
+{
+    return reply->hasRawHeader(QByteArray(header));
+}
+#else
+#include <QNetworkReply>
+inline bool QNetworkReplyHasRawHeader(QNetworkReply *reply, const char *header)
+{
+    return reply->hasRawHeader(header);
+}
+#endif
+
+//-----------------------------------------------------------------------------
+
+#endif // QT_NETWORK_LIB
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
+
+#ifdef QT_CORE_LIB
+
+//-----------------------------------------------------------------------------
+
+#include <QJsonValue>
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 8, 0)
+inline bool operator==(const QString &lhs, const QJsonValue &rhs)
+{
+    return lhs == rhs.toString();
+}
+
+inline bool operator==(const QJsonValue &lhs, const QString &rhs)
+{
+    return lhs.toString() == rhs;
+}
+
+inline bool operator!=(const QString &lhs, const QJsonValue &rhs)
+{
+    return lhs != rhs.toString();
+}
+
+inline bool operator!=(const QJsonValue &lhs, const QString &rhs)
+{
+    return lhs.toString() != rhs;
+}
+
+// QJsonValueRef is a non-const reference to a QJsonValue inside QJsonObject/QJsonArray
+// It can be implicitly converted to QJsonValue, but we need explicit operators
+// to avoid ambiguity when comparing with QString
+inline bool operator==(const QString &lhs, const QJsonValueRef &rhs)
+{
+    return lhs == rhs.toString();
+}
+
+inline bool operator==(const QJsonValueRef &lhs, const QString &rhs)
+{
+    return lhs.toString() == rhs;
+}
+
+inline bool operator!=(const QString &lhs, const QJsonValueRef &rhs)
+{
+    return lhs != rhs.toString();
+}
+
+inline bool operator!=(const QJsonValueRef &lhs, const QString &rhs)
+{
+    return lhs.toString() != rhs;
+}
+#endif
+
+//-----------------------------------------------------------------------------
+
+#endif // QT_CORE_LIB
+
+//-----------------------------------------------------------------------------
+
+#ifdef QT_QML_LIB
+
+//-----------------------------------------------------------------------------
+
+#include <QQmlComponent>
+#include <QQmlEngine>
+#include <QUrl>
+
+//-----------------------------------------------------------------------------
+
+#if QT_VERSION<QT_VERSION_CHECK(6, 5, 0)
+namespace {
+inline QUrl qmlComponentUrlFromUri(const QString &uri, const QString &typeName)
+{
+    // Convert uri like "com.nextcloud.desktopclient.search" to qrc path
+    // like "qrc:/qt/qml/com/nextcloud/desktopclient/search/SearchWindow.qml"
+    auto path = uri;
+    path.replace(QLatin1Char('.'), QLatin1Char('/'));
+    return QUrl(QStringLiteral("qrc:/qt/qml/") + path + u'/' + typeName + QStringLiteral(".qml"));
+}
+}
+
+inline QQmlComponent *QmlComponentFromUri(QQmlEngine *engine, const QString &uri, const QString &typeName)
+{
+    return new QQmlComponent(engine, qmlComponentUrlFromUri(uri, typeName));
+}
+#else
+inline QQmlComponent *QmlComponentFromUri(QQmlEngine *engine, const QString &uri, const QString &typeName)
+{
+    return new QQmlComponent(engine, uri, typeName);
+}
+#endif
+
+//-----------------------------------------------------------------------------
+
+#endif // QT_QML_LIB
+
+//-----------------------------------------------------------------------------
+//-----------------------------------------------------------------------------
diff --git a/src/common/syncjournaldb.cpp b/src/common/syncjournaldb.cpp
index acd97717c0..3249befd46 100644
--- a/src/common/syncjournaldb.cpp
+++ b/src/common/syncjournaldb.cpp
@@ -28,6 +28,8 @@
 
 #include <cstring>
 
+#include "common/qtcompat.h"
+
 // SQL expression to check whether path.startswith(prefix + '/')
 // Note: '/' + 1 == '0'
 #define IS_PREFIX_PATH_OF(prefix, path) \
@@ -3354,4 +3356,4 @@ QDebug& operator<<(QDebug &stream, const SyncJournalFileRecord::EncryptionStatus
 
 } // namespace OCC
 
-#include "moc_syncjournaldb.cpp"
\ No newline at end of file
+#include "moc_syncjournaldb.cpp"
diff --git a/src/csync/csync_exclude.cpp b/src/csync/csync_exclude.cpp
index a48d341c2e..09577a4eee 100644
--- a/src/csync/csync_exclude.cpp
+++ b/src/csync/csync_exclude.cpp
@@ -21,6 +21,7 @@
 
 #include "common/utility.h"
 #include "common/filesystembase.h"
+#include "common/qtcompat.h"
 #include "../version.h"
 
 #include <QString>
@@ -316,7 +317,7 @@ void ExcludedFiles::loadExcludeFilePatterns(const QString &basePath, QFile &file
             continue;
         }
         const auto patternStr = QString::fromUtf8(line);
-        if (QStringView{patternStr}.trimmed() == QLatin1StringView("*")) {
+        if (QStringView{patternStr}.trimmed() == QLatin1StringOrView("*")) {
             continue;
         }
         csync_exclude_expand_escapes(line);
@@ -489,10 +490,10 @@ CSYNC_EXCLUDE_TYPE ExcludedFiles::traversalPatternMatch(const QString &path, Ite
         QRegularExpressionMatch m;
         if (filetype == ItemTypeDirectory
             && _bnameTraversalRegexDir.contains(basePath)) {
-            m = _bnameTraversalRegexDir[basePath].matchView(bnameStr);
+            m = matchRegularExpression(_bnameTraversalRegexDir[basePath], bnameStr);
         } else if (filetype == ItemTypeFile
             && _bnameTraversalRegexFile.contains(basePath)) {
-            m = _bnameTraversalRegexFile[basePath].matchView(bnameStr);
+            m = matchRegularExpression(_bnameTraversalRegexFile[basePath], bnameStr);
         } else {
             continue;
         }
diff --git a/src/gui/CMakeLists.txt b/src/gui/CMakeLists.txt
index c708f20e5e..3daf1197c1 100644
--- a/src/gui/CMakeLists.txt
+++ b/src/gui/CMakeLists.txt
@@ -3,7 +3,7 @@
 # SPDX-License-Identifier: GPL-2.0-or-later
 project(gui)
 find_package(Qt${QT_MAJOR_VERSION} REQUIRED COMPONENTS Widgets Svg Qml Quick QuickControls2 QuickWidgets Xml Network)
-find_package(KF6Archive REQUIRED)
+find_package(KF6Archive)
 find_package(KF6GuiAddons)
 
 if(CMAKE_BUILD_TYPE MATCHES Debug)
@@ -286,6 +286,12 @@ IF(BUILD_UPDATER)
     )
 endif()
 
+if ("${QT_VERSION_MAJOR}.${QT_VERSION_MINOR}" VERSION_GREATER_EQUAL "6.4")
+    set(QML_COMPONENT_BEHAVIOR_PRAGMA "pragma ComponentBehavior: Bound")
+else()
+    set(QML_COMPONENT_BEHAVIOR_PRAGMA "")
+endif()
+
 add_subdirectory(search)
 add_subdirectory(sharing)
 include(assistant/CMakeLists.txt)
@@ -587,10 +593,17 @@ target_link_libraries(nextcloudCore
   Qt::Quick
   Qt::QuickControls2
   Qt::QuickWidgets
-  KF6::Archive
   KDAB::kdsingleapplication
   )
 
+if(KF6Archive_FOUND)
+  target_link_libraries(nextcloudCore
+    PUBLIC
+    KF6::Archive
+  )
+  add_definitions(-DHAVE_KARCHIVE)
+endif()
+
 if(KF6GuiAddons_FOUND)
   target_link_libraries(nextcloudCore
     PUBLIC
@@ -854,3 +867,120 @@ if(NOT BUILD_OWNCLOUD_OSX_BUNDLE AND NOT WIN32)
 endif()
 
 configure_file(configgui.h.in ${CMAKE_CURRENT_BINARY_DIR}/configgui.h)
+
+if ("${QT_VERSION_MAJOR}.${QT_VERSION_MINOR}" VERSION_GREATER_EQUAL "6.6")
+    set(QML_UNIFORM_CELL_SIZES_TRUE "uniformCellSizes: true")
+else()
+    set(QML_UNIFORM_CELL_SIZES_TRUE "")
+endif()
+configure_file(wizard/qml/BrowserAuthFooter.qml.in ${CMAKE_CURRENT_SOURCE_DIR}/wizard/qml/BrowserAuthFooter.qml)
+
+if ("${QT_VERSION_MAJOR}.${QT_VERSION_MINOR}" VERSION_GREATER_EQUAL "6.9")
+    set(QML_NC_CONTEXT_MENU_TEXT_FIELD  [=[
+    ContextMenu.menu: Menu {
+        // Match AutoSizingMenu: render the menu in its own window so that its
+        // items receive mouse events correctly inside frameless windows.
+        popupType: Popup.Window
+
+        MenuItem {
+            text: qsTr("Undo")
+            enabled: root.canUndo && !root.readOnly
+            onTriggered: root.undo()
+        }
+
+        MenuItem {
+            text: qsTr("Redo")
+            enabled: root.canRedo && !root.readOnly
+            onTriggered: root.redo()
+        }
+
+        MenuSeparator {}
+
+        MenuItem {
+            text: qsTr("Cut")
+            enabled: root.selectedText.length > 0 && root.echoMode === TextInput.Normal && !root.readOnly
+            onTriggered: root.cut()
+        }
+
+        MenuItem {
+            text: qsTr("Copy")
+            enabled: root.selectedText.length > 0 && root.echoMode === TextInput.Normal
+            onTriggered: root.copy()
+        }
+
+        MenuItem {
+            text: qsTr("Paste")
+            enabled: root.canPaste && !root.readOnly
+            onTriggered: root.paste()
+        }
+
+        MenuItem {
+            text: qsTr("Delete")
+            enabled: root.selectedText.length > 0 && !root.readOnly
+            onTriggered: root.remove(root.selectionStart, root.selectionEnd)
+        }
+
+        MenuSeparator {}
+
+        MenuItem {
+            text: qsTr("Select All")
+            enabled: root.length > 0
+            onTriggered: root.selectAll()
+        }
+    }
+]=])
+else()
+    set(QML_NC_CONTEXT_MENU_TEXT_FIELD "")
+endif()
+configure_file(common/NCContextMenuTextField.qml.in ${CMAKE_CURRENT_SOURCE_DIR}/common/NCContextMenuTextField.qml)
+
+if ("${QT_VERSION_MAJOR}.${QT_VERSION_MINOR}" VERSION_GREATER_EQUAL "6.5")
+    set(QML_WIZARD_ICON_TINT_IMPORT "import QtQuick.Effects")
+    set(QML_WIZARD_BUTTON_ICON_TINT_EFFECT [=[
+            MultiEffect {
+                objectName: "wizardButtonLeadingIconTint"
+                anchors.fill: leadingIconImage
+                visible: root.tintIcon
+                source: leadingIconImage
+                colorization: 1.0
+                colorizationColor: root.iconTintColor
+                Accessible.ignored: true
+            }
+]=])
+    set(QML_WIZARD_MENU_ITEM_ICON_TINT_EFFECT [=[
+            MultiEffect {
+                objectName: "wizardMenuItemIconTint"
+                anchors.fill: menuIconImage
+                visible: root.tintIcon
+                source: menuIconImage
+                colorization: 1.0
+                colorizationColor: root.iconTintColor
+                Accessible.ignored: true
+            }
+]=])
+else()
+    set(QML_WIZARD_ICON_TINT_IMPORT "import Qt5Compat.GraphicalEffects")
+    set(QML_WIZARD_BUTTON_ICON_TINT_EFFECT [=[
+            ColorOverlay {
+                objectName: "wizardButtonLeadingIconTint"
+                anchors.fill: leadingIconImage
+                visible: root.tintIcon
+                source: leadingIconImage
+                color: root.iconTintColor
+                Accessible.ignored: true
+            }
+]=])
+    set(QML_WIZARD_MENU_ITEM_ICON_TINT_EFFECT [=[
+            ColorOverlay {
+                objectName: "wizardMenuItemIconTint"
+                anchors.fill: menuIconImage
+                visible: root.tintIcon
+                source: menuIconImage
+                color: root.iconTintColor
+                Accessible.ignored: true
+            }
+]=])
+endif()
+configure_file(wizard/qml/WizardButton.qml.in ${CMAKE_CURRENT_SOURCE_DIR}/wizard/qml/WizardButton.qml)
+configure_file(wizard/qml/WizardMenuItem.qml.in ${CMAKE_CURRENT_SOURCE_DIR}/wizard/qml/WizardMenuItem.qml)
+configure_file(integration/FileActionsWindow.qml.in ${CMAKE_CURRENT_SOURCE_DIR}/integration/FileActionsWindow.qml)
diff --git a/src/gui/EmojiPicker.qml b/src/gui/EmojiPicker.qml
index ca1026e419..9252c314fb 100644
--- a/src/gui/EmojiPicker.qml
+++ b/src/gui/EmojiPicker.qml
@@ -180,10 +180,18 @@ ColumnLayout {
                 }
 
                 ToolTip {
-                    popupType: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+                    id: emojiDelegateToolTip
+
                     text: modelData === undefined ? "" : modelData.shortname
                     visible: emojiDelegate.hovered
                     delay: Qt.styleHints.mousePressAndHoldInterval
+
+                    Binding {
+                        target: emojiDelegateToolTip
+                        property: "popupType"
+                        value: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+                        when: emojiDelegateToolTip.hasOwnProperty("popupType")
+                    }
                 }
 
                 onClicked: {
diff --git a/src/gui/accountsettings.cpp b/src/gui/accountsettings.cpp
index cfd3cb59ab..e66b8dab8e 100644
--- a/src/gui/accountsettings.cpp
+++ b/src/gui/accountsettings.cpp
@@ -10,6 +10,7 @@
 #include "common/syncjournalfilerecord.h"
 #include "qmessagebox.h"
 #include "ui_accountsettings.h"
+#include "common/qtcompat.h"
 
 #include "theme.h"
 #include "foldercreationdialog.h"
diff --git a/src/gui/accountsetupfromcommandlinejob.cpp b/src/gui/accountsetupfromcommandlinejob.cpp
index 0ad321754d..3d80caa918 100644
--- a/src/gui/accountsetupfromcommandlinejob.cpp
+++ b/src/gui/accountsetupfromcommandlinejob.cpp
@@ -5,6 +5,8 @@
 
 #include "accountsetupfromcommandlinejob.h"
 
+#include "common/qtcompat.h"
+
 #include "accountmanager.h"
 #include "accountstate.h"
 #include "common/filesystembase.h"
diff --git a/src/gui/accountstate.cpp b/src/gui/accountstate.cpp
index c9cf0a6e5a..0dec40e1e1 100644
--- a/src/gui/accountstate.cpp
+++ b/src/gui/accountstate.cpp
@@ -18,6 +18,7 @@
 #include "pushnotifications.h"
 #include "networkjobs.h"
 #include "settings/migration.h"
+#include "common/qtcompat.h"
 
 #include <QSettings>
 #include <QTimer>
diff --git a/src/gui/activity/activitydata.cpp b/src/gui/activity/activitydata.cpp
index 8d41c85cdd..9f9395e84f 100644
--- a/src/gui/activity/activitydata.cpp
+++ b/src/gui/activity/activitydata.cpp
@@ -8,6 +8,7 @@
 
 #include "activitydata.h"
 #include "folderman.h"
+#include "common/qtcompat.h"
 
 using namespace Qt::StringLiterals;
 
diff --git a/src/gui/activity/activitylistmodel.cpp b/src/gui/activity/activitylistmodel.cpp
index 8a3285122c..2b865040dc 100644
--- a/src/gui/activity/activitylistmodel.cpp
+++ b/src/gui/activity/activitylistmodel.cpp
@@ -19,6 +19,7 @@
 #include "systray.h"
 #include "talkreply.h"
 #include "common/utility.h"
+#include "common/qtcompat.h"
 
 #include <QAbstractListModel>
 #include <QDesktopServices>
diff --git a/src/gui/activity/notificationhandler.cpp b/src/gui/activity/notificationhandler.cpp
index 77547a021f..012fba7ab3 100644
--- a/src/gui/activity/notificationhandler.cpp
+++ b/src/gui/activity/notificationhandler.cpp
@@ -8,6 +8,7 @@
 #include "accountstate.h"
 #include "capabilities.h"
 #include "networkjobs.h"
+#include "common/qtcompat.h"
 
 #include "iconjob.h"
 
diff --git a/src/gui/activity/qml/ActivityFileMenuButton.qml b/src/gui/activity/qml/ActivityFileMenuButton.qml
index 5e6d16afd1..f55d2173c6 100644
--- a/src/gui/activity/qml/ActivityFileMenuButton.qml
+++ b/src/gui/activity/qml/ActivityFileMenuButton.qml
@@ -30,9 +30,17 @@ Button {
     icon.height: buttonIconSize
 
     ToolTip {
-        popupType: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+        id: buttonTooltip
+
         text: qsTr("Open file details")
         visible: parent.hovered
+
+        Binding {
+            target: buttonTooltip
+            property: "popupType"
+            value: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+            when: buttonTooltip.hasOwnProperty("popupType")
+        }
     }
 
     display: Button.IconOnly
diff --git a/src/gui/activity/qml/ActivityItem.qml b/src/gui/activity/qml/ActivityItem.qml
index 34d5176bb5..85f97b4a97 100644
--- a/src/gui/activity/qml/ActivityItem.qml
+++ b/src/gui/activity/qml/ActivityItem.qml
@@ -12,6 +12,9 @@ import Style
 ItemDelegate {
     id: root
 
+    required property var model
+    required property int index
+
     property Flickable flickable
 
     property int iconSize: Style.trayListItemIconSize
@@ -33,9 +36,17 @@ ItemDelegate {
     Accessible.onPressAction: root.clicked()
 
     ToolTip {
-        popupType: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+        id: rootToolTip
+
         visible: root.hovered && !activityContent.childHovered && model.displayLocation !== ""
         text: qsTr("In %1").arg(model.displayLocation)
+
+        Binding {
+            target: rootToolTip
+            property: "popupType"
+            value: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+            when: rootToolTip.hasOwnProperty("popupType")
+        }
     }
 
     // TODO: the current style does not support customization of this control
@@ -49,7 +60,6 @@ ItemDelegate {
 
             Layout.fillWidth: true
             Layout.minimumHeight: Style.minActivityHeight
-            Layout.preferredWidth: parent.width
 
             showDismissButton: model.isDismissable
 
diff --git a/src/gui/activity/qml/ActivityItemContent.qml b/src/gui/activity/qml/ActivityItemContent.qml
index 253c164e7c..f7c6eb0ae1 100644
--- a/src/gui/activity/qml/ActivityItemContent.qml
+++ b/src/gui/activity/qml/ActivityItemContent.qml
@@ -15,7 +15,7 @@ import "../../tray"
 RowLayout {
     id: root
 
-    required property color adaptiveTextColor
+    property color adaptiveTextColor
 
     property variant activityData: {{}}
 
@@ -126,8 +126,6 @@ RowLayout {
 
         Layout.fillHeight: true
         Layout.fillWidth: true
-        Layout.maximumWidth: root.width - Style.standardSpacing - root.iconSize
-        implicitWidth: root.width - Style.standardSpacing - root.iconSize
 
         spacing: Style.smallSpacing
 
@@ -199,9 +197,17 @@ RowLayout {
                     display: Button.IconOnly
 
                     ToolTip {
-                        popupType: Qt.platform.os === "windows" ? Popup.Item : Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+                        id: dismissActionButtonToolTip
+
                         text: qsTr("Dismiss")
                         visible: parent.hovered
+
+                        Binding {
+                            target: dismissActionButtonToolTip
+                            property: "popupType"
+                            value: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+                            when: dismissActionButtonToolTip.hasOwnProperty("popupType")
+                        }
                     }
 
                     visible: root.showDismissButton
diff --git a/src/gui/activity/qml/TalkReplyTextField.qml b/src/gui/activity/qml/TalkReplyTextField.qml
index 049a6da2eb..50c2fd68b1 100644
--- a/src/gui/activity/qml/TalkReplyTextField.qml
+++ b/src/gui/activity/qml/TalkReplyTextField.qml
@@ -54,9 +54,17 @@ NCContextMenuTextField {
         }
 
         ToolTip {
-            popupType: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+            id: sendReplyMessageButtonToolTip
+
             visible: sendReplyMessageButton.hovered
             text:  qsTr("Send reply to chat message")
+
+            Binding {
+                target: sendReplyMessageButtonToolTip
+                property: "popupType"
+                value: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+                when: sendReplyMessageButtonToolTip.hasOwnProperty("popupType")
+            }
         }
     }
 }
diff --git a/src/gui/activity/talkreply.cpp b/src/gui/activity/talkreply.cpp
index bc21faee16..465ebb62a7 100644
--- a/src/gui/activity/talkreply.cpp
+++ b/src/gui/activity/talkreply.cpp
@@ -7,6 +7,7 @@
 
 #include "accountstate.h"
 #include "networkjobs.h"
+#include "common/qtcompat.h"
 
 #include <QJsonDocument>
 #include <QJsonObject>
diff --git a/src/gui/advancedsettings.cpp b/src/gui/advancedsettings.cpp
index 60d024b158..a2b7fc790f 100644
--- a/src/gui/advancedsettings.cpp
+++ b/src/gui/advancedsettings.cpp
@@ -28,7 +28,9 @@
 #include "common/utility_mac_sandbox.h"
 #endif
 
+#ifdef HAVE_KARCHIVE
 #include <KZip>
+#endif
 
 #include <QDir>
 #include <QDirIterator>
@@ -122,6 +124,7 @@ QVector<ZipEntry> createDebugArchiveFileList()
     return list;
 }
 
+#ifdef HAVE_KARCHIVE
 bool createDebugArchive(const QString &filename)
 {
     const auto entries = createDebugArchiveFileList();
@@ -222,6 +225,8 @@ bool createDebugArchive(const QString &filename)
     return true;
 }
 
+#endif // HAVE_KARCHIVE
+
 } // namespace
 
 namespace OCC {
@@ -389,6 +394,7 @@ void AdvancedSettings::slotCreateDebugArchive()
         return;
     }
 
+#ifdef HAVE_KARCHIVE
 #ifdef Q_OS_MACOS
     auto scopedAccess = Utility::MacSandboxSecurityScopedAccess::create(destination);
 
@@ -409,6 +415,13 @@ void AdvancedSettings::slotCreateDebugArchive()
             tr("Redact information deemed sensitive before sharing! Debug archive created at %1").arg(destination.toLocalFile())
         );
     }
+#else
+    QMessageBox::information(
+        this,
+        tr("Debug Archive Not Available"),
+        tr("Debug archive creation is not available on this platform.")
+    );
+#endif
 }
 
 void AdvancedSettings::slotRemotePollIntervalChanged(int seconds)
diff --git a/src/gui/application.cpp b/src/gui/application.cpp
index d9bddcbebe..c673f19b98 100644
--- a/src/gui/application.cpp
+++ b/src/gui/application.cpp
@@ -32,6 +32,7 @@
 #include "csync_exclude.h"
 #include "owncloudsetupwizard.h"
 #include "version.h"
+#include "common/qtcompat.h"
 
 #include "config.h"
 #include "settings/migration.h"
@@ -1264,7 +1265,7 @@ void Application::setupTranslations()
     qCInfo(lcApplication) << "System UI languages are:" << QLocale::system().uiLanguages();
     auto choosenLanguage = enforcedLanguage();
     if (choosenLanguage.isEmpty()) {
-        for(const auto &localeToTest : QLocale::system().uiLanguages(QLocale::TagSeparator::Underscore)){
+        for(const auto &localeToTest : QLocale_uiLanguages()){
             const auto trFile = QString{QLatin1String{"client_"} + localeToTest};
             qCDebug(lcApplication()) << "trying to load" << localeToTest << "in" << trFile << "from" << trPath;
             if (translator->load(trFile, trPath)) {
diff --git a/src/gui/assistant/assistantapijob.cpp b/src/gui/assistant/assistantapijob.cpp
index bf2944e618..b8b98ba04d 100644
--- a/src/gui/assistant/assistantapijob.cpp
+++ b/src/gui/assistant/assistantapijob.cpp
@@ -5,6 +5,8 @@
 
 #include "assistantapijob.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonDocument>
 #include <QLoggingCategory>
 #include <QNetworkReply>
diff --git a/src/gui/assistant/assistantclient.cpp b/src/gui/assistant/assistantclient.cpp
index 1883372889..ff8c6d45f7 100644
--- a/src/gui/assistant/assistantclient.cpp
+++ b/src/gui/assistant/assistantclient.cpp
@@ -8,6 +8,8 @@
 #include "networkjobs.h"
 #include "ocsassistantconnector.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonObject>
 #include <QUrlQuery>
 
diff --git a/src/gui/assistant/assistantcontroller.cpp b/src/gui/assistant/assistantcontroller.cpp
index 9e0935b753..c673a5afdd 100644
--- a/src/gui/assistant/assistantcontroller.cpp
+++ b/src/gui/assistant/assistantcontroller.cpp
@@ -9,6 +9,8 @@
 #include "assistantclient.h"
 #include "assistantutils.h"
 
+#include "common/qtcompat.h"
+
 #include <QDateTime>
 #include <QJsonArray>
 #include <QJsonObject>
diff --git a/src/gui/assistant/assistantconversationmodel.cpp b/src/gui/assistant/assistantconversationmodel.cpp
index e7d5b39362..6aedb61c0c 100644
--- a/src/gui/assistant/assistantconversationmodel.cpp
+++ b/src/gui/assistant/assistantconversationmodel.cpp
@@ -7,6 +7,8 @@
 
 #include "assistantutils.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonArray>
 
 #include <algorithm>
diff --git a/src/gui/assistant/assistantmessagemodel.cpp b/src/gui/assistant/assistantmessagemodel.cpp
index 0375c9645f..e88e6440bc 100644
--- a/src/gui/assistant/assistantmessagemodel.cpp
+++ b/src/gui/assistant/assistantmessagemodel.cpp
@@ -7,6 +7,8 @@
 
 #include "assistantutils.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonArray>
 
 #include <utility>
diff --git a/src/gui/assistant/assistanttaskmodel.cpp b/src/gui/assistant/assistanttaskmodel.cpp
index d553a1ce3f..429c9b1dfe 100644
--- a/src/gui/assistant/assistanttaskmodel.cpp
+++ b/src/gui/assistant/assistanttaskmodel.cpp
@@ -7,6 +7,8 @@
 
 #include "assistantutils.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonArray>
 #include <QJsonObject>
 
diff --git a/src/gui/assistant/assistanttasktypemodel.cpp b/src/gui/assistant/assistanttasktypemodel.cpp
index 3871db4e93..e5a9a23356 100644
--- a/src/gui/assistant/assistanttasktypemodel.cpp
+++ b/src/gui/assistant/assistanttasktypemodel.cpp
@@ -5,6 +5,8 @@
 
 #include "assistanttasktypemodel.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonObject>
 #include <QLoggingCategory>
 
diff --git a/src/gui/assistant/assistantutils.cpp b/src/gui/assistant/assistantutils.cpp
index bffba440b8..1c4a3a2ab6 100644
--- a/src/gui/assistant/assistantutils.cpp
+++ b/src/gui/assistant/assistantutils.cpp
@@ -5,6 +5,8 @@
 
 #include "assistantutils.h"
 
+#include "common/qtcompat.h"
+
 #include <QDateTime>
 #include <QJsonObject>
 #include <QLocale>
diff --git a/src/gui/assistant/ocsassistantconnector.cpp b/src/gui/assistant/ocsassistantconnector.cpp
index 215a999c07..c0344290e5 100644
--- a/src/gui/assistant/ocsassistantconnector.cpp
+++ b/src/gui/assistant/ocsassistantconnector.cpp
@@ -9,6 +9,8 @@
 #include "assistantapijob.h"
 #include "networkjobs.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonDocument>
 #include <QJsonObject>
 #include <QLoggingCategory>
diff --git a/src/gui/common/NCContextMenuTextField.qml b/src/gui/common/NCContextMenuTextField.qml
deleted file mode 100644
index a83796ce64..0000000000
--- a/src/gui/common/NCContextMenuTextField.qml
+++ /dev/null
@@ -1,65 +0,0 @@
-/*
- * SPDX-FileCopyrightText: 2026 Nextcloud GmbH and Nextcloud contributors
- * SPDX-License-Identifier: GPL-2.0-or-later
- */
-
-import QtQuick
-import QtQuick.Controls
-
-TextField {
-    id: root
-
-    selectByMouse: true
-
-    ContextMenu.menu: Menu {
-        // Match AutoSizingMenu: render the menu in its own window so that its
-        // items receive mouse events correctly inside frameless windows.
-        popupType: Popup.Window
-
-        MenuItem {
-            text: qsTr("Undo")
-            enabled: root.canUndo && !root.readOnly
-            onTriggered: root.undo()
-        }
-
-        MenuItem {
-            text: qsTr("Redo")
-            enabled: root.canRedo && !root.readOnly
-            onTriggered: root.redo()
-        }
-
-        MenuSeparator {}
-
-        MenuItem {
-            text: qsTr("Cut")
-            enabled: root.selectedText.length > 0 && root.echoMode === TextInput.Normal && !root.readOnly
-            onTriggered: root.cut()
-        }
-
-        MenuItem {
-            text: qsTr("Copy")
-            enabled: root.selectedText.length > 0 && root.echoMode === TextInput.Normal
-            onTriggered: root.copy()
-        }
-
-        MenuItem {
-            text: qsTr("Paste")
-            enabled: root.canPaste && !root.readOnly
-            onTriggered: root.paste()
-        }
-
-        MenuItem {
-            text: qsTr("Delete")
-            enabled: root.selectedText.length > 0 && !root.readOnly
-            onTriggered: root.remove(root.selectionStart, root.selectionEnd)
-        }
-
-        MenuSeparator {}
-
-        MenuItem {
-            text: qsTr("Select All")
-            enabled: root.length > 0
-            onTriggered: root.selectAll()
-        }
-    }
-}
diff --git a/src/gui/common/NCContextMenuTextField.qml.in b/src/gui/common/NCContextMenuTextField.qml.in
new file mode 100644
index 0000000000..3af9d5c90e
--- /dev/null
+++ b/src/gui/common/NCContextMenuTextField.qml.in
@@ -0,0 +1,15 @@
+/*
+ * SPDX-FileCopyrightText: 2026 Nextcloud GmbH and Nextcloud contributors
+ * SPDX-License-Identifier: GPL-2.0-or-later
+ */
+
+import QtQuick
+import QtQuick.Controls
+
+TextField {
+    id: root
+
+    selectByMouse: true
+
+@QML_NC_CONTEXT_MENU_TEXT_FIELD@
+}
diff --git a/src/gui/filedetails/FileDetailsPage.qml b/src/gui/filedetails/FileDetailsPage.qml
index c061eff726..2a0751b07c 100644
--- a/src/gui/filedetails/FileDetailsPage.qml
+++ b/src/gui/filedetails/FileDetailsPage.qml
@@ -196,9 +196,17 @@ Page {
                     }
 
                     ToolTip {
-                        popupType: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+                        id: overflowTagToolTip
+
                         visible: hoverHandler.hovered
                         text: tagRepeater.fileTagModel.overflowTagsString
+
+                        Binding {
+                            target: overflowTagToolTip
+                            property: "popupType"
+                            value: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+                            when: overflowTagToolTip.hasOwnProperty("popupType")
+                        }
                     }
                 }
             }
diff --git a/src/gui/folderman.cpp b/src/gui/folderman.cpp
index 8458f0438f..9c4e785344 100644
--- a/src/gui/folderman.cpp
+++ b/src/gui/folderman.cpp
@@ -16,6 +16,7 @@
 #include "filesystem.h"
 #include "lockwatcher.h"
 #include "common/asserts.h"
+#include "common/qtcompat.h"
 #include "gui/systray.h"
 #include "logger.h"
 #include <pushnotifications.h>
diff --git a/src/gui/folderstatusmodel.cpp b/src/gui/folderstatusmodel.cpp
index fb82fe4057..9b026d6f63 100644
--- a/src/gui/folderstatusmodel.cpp
+++ b/src/gui/folderstatusmodel.cpp
@@ -8,6 +8,7 @@
 #include "accountstate.h"
 #include "common/asserts.h"
 #include "common/utility.h"
+#include "common/qtcompat.h"
 #include "folderman.h"
 #include "folderstatusdelegate.h"
 #include <account.h>
diff --git a/src/gui/folderwizard.cpp b/src/gui/folderwizard.cpp
index 40a2080aa7..e0481275c4 100644
--- a/src/gui/folderwizard.cpp
+++ b/src/gui/folderwizard.cpp
@@ -15,6 +15,7 @@
 #include "creds/abstractcredentials.h"
 #include "guiutility.h"
 #include "common/asserts.h"
+#include "common/qtcompat.h"
 
 #ifdef Q_OS_MACOS
 #include "common/utility_mac_sandbox.h"
@@ -589,7 +590,7 @@ FolderWizardSelectiveSync::FolderWizardSelectiveSync(const AccountPtr &account)
     if (!Theme::instance()->disableVirtualFilesSyncFolder() && Theme::instance()->showVirtualFilesOption() && bestAvailableVfsMode() != Vfs::Off) {
         _virtualFilesCheckBox = new QCheckBox(tr("Use virtual files instead of downloading content immediately %1").arg(bestAvailableVfsMode() == Vfs::WindowsCfApi ? QString() : tr("(experimental)")));
         connect(_virtualFilesCheckBox, &QCheckBox::clicked, this, &FolderWizardSelectiveSync::virtualFilesCheckboxClicked);
-        connect(_virtualFilesCheckBox, &QCheckBox::checkStateChanged, this, [this](int state) {
+        connectCheckBoxStateChanged(_virtualFilesCheckBox, this, [this](int state) {
             _selectiveSync->setEnabled(state == Qt::Unchecked);
         });
         _virtualFilesCheckBox->setChecked(bestAvailableVfsMode() == Vfs::WindowsCfApi);
diff --git a/src/gui/governance/applygovernancelabel.cpp b/src/gui/governance/applygovernancelabel.cpp
index 88dc726d78..0a97b6b15c 100644
--- a/src/gui/governance/applygovernancelabel.cpp
+++ b/src/gui/governance/applygovernancelabel.cpp
@@ -7,6 +7,8 @@
 
 #include "ocsgovernancejob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC
diff --git a/src/gui/governance/deletegovernancelabel.cpp b/src/gui/governance/deletegovernancelabel.cpp
index 56032e2a64..e32545ada1 100644
--- a/src/gui/governance/deletegovernancelabel.cpp
+++ b/src/gui/governance/deletegovernancelabel.cpp
@@ -7,6 +7,8 @@
 
 #include "ocsgovernancejob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC
diff --git a/src/gui/governance/getavailablegovernancelabels.cpp b/src/gui/governance/getavailablegovernancelabels.cpp
index 310fd8471d..9e20883246 100644
--- a/src/gui/governance/getavailablegovernancelabels.cpp
+++ b/src/gui/governance/getavailablegovernancelabels.cpp
@@ -7,6 +7,8 @@
 
 #include "ocsgovernancejob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC
diff --git a/src/gui/governance/getgovernancelabels.cpp b/src/gui/governance/getgovernancelabels.cpp
index ac95577a50..8759c03264 100644
--- a/src/gui/governance/getgovernancelabels.cpp
+++ b/src/gui/governance/getgovernancelabels.cpp
@@ -7,6 +7,8 @@
 
 #include "ocsgovernancejob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC
diff --git a/src/gui/governance/governancelabelslistmodel.cpp b/src/gui/governance/governancelabelslistmodel.cpp
index 310ae11a29..d4ec044c2c 100644
--- a/src/gui/governance/governancelabelslistmodel.cpp
+++ b/src/gui/governance/governancelabelslistmodel.cpp
@@ -13,6 +13,8 @@
 #include <QJsonObject>
 #include <QJsonArray>
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC
diff --git a/src/gui/governance/governancenetworkjob.cpp b/src/gui/governance/governancenetworkjob.cpp
index e43d2824c8..a80ad0311b 100644
--- a/src/gui/governance/governancenetworkjob.cpp
+++ b/src/gui/governance/governancenetworkjob.cpp
@@ -5,6 +5,8 @@
 
 #include "governancenetworkjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC
diff --git a/src/gui/governance/typedgovernancenetworkjob.cpp b/src/gui/governance/typedgovernancenetworkjob.cpp
index 7ac3ce6be5..334da54790 100644
--- a/src/gui/governance/typedgovernancenetworkjob.cpp
+++ b/src/gui/governance/typedgovernancenetworkjob.cpp
@@ -5,6 +5,8 @@
 
 #include "typedgovernancenetworkjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC
diff --git a/src/gui/governance/typedwithlabelidgovernancenetworkjob.cpp b/src/gui/governance/typedwithlabelidgovernancenetworkjob.cpp
index 81102ff000..716485424d 100644
--- a/src/gui/governance/typedwithlabelidgovernancenetworkjob.cpp
+++ b/src/gui/governance/typedwithlabelidgovernancenetworkjob.cpp
@@ -5,6 +5,8 @@
 
 #include "typedwithlabelidgovernancenetworkjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC
diff --git a/src/gui/integration/FileActionsWindow.qml b/src/gui/integration/FileActionsWindow.qml.in
similarity index 98%
rename from src/gui/integration/FileActionsWindow.qml
rename to src/gui/integration/FileActionsWindow.qml.in
index 5b8325647e..cb66b59028 100644
--- a/src/gui/integration/FileActionsWindow.qml
+++ b/src/gui/integration/FileActionsWindow.qml.in
@@ -3,7 +3,7 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
-pragma ComponentBehavior: Bound
+@QML_COMPONENT_BEHAVIOR_PRAGMA@
 
 import QtQuick
 import QtQuick.Window
@@ -150,7 +150,6 @@ ApplicationWindow {
                         color: palette.windowText
                         width: Style.minimumActivityItemHeight
                         height: Style.minimumActivityItemHeight
-                        Layout.alignment: Qt.AlignHCenter | Qt.AlignBottom
                     }
 
                     background: Rectangle {
diff --git a/src/gui/owncloudsetupwizard.cpp b/src/gui/owncloudsetupwizard.cpp
index 2516bb16d2..2de5f225fe 100644
--- a/src/gui/owncloudsetupwizard.cpp
+++ b/src/gui/owncloudsetupwizard.cpp
@@ -4,6 +4,7 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
+#include "common/qtcompat.h"
 #include "owncloudsetupwizard.h"
 
 #include "accountmanager.h"
diff --git a/src/gui/search/CMakeLists.txt b/src/gui/search/CMakeLists.txt
index 369da1cf08..ea6b947c84 100644
--- a/src/gui/search/CMakeLists.txt
+++ b/src/gui/search/CMakeLists.txt
@@ -1,8 +1,26 @@
 # SPDX-FileCopyrightText: 2026 Nextcloud GmbH and Nextcloud contributors
 # SPDX-License-Identifier: GPL-2.0-or-later
 
+set(CMAKE_AUTOMOC ON)
+
 add_library(nextcloudGuiSearch STATIC)
 
+foreach(_search_component_behavior_file UnifiedSearchFilterBar.qml UnifiedSearchPeoplePopup.qml)
+  configure_file(${_search_component_behavior_file}.in ${CMAKE_CURRENT_SOURCE_DIR}/${_search_component_behavior_file})
+endforeach()
+
+if ("${QT_VERSION_MAJOR}.${QT_VERSION_MINOR}" VERSION_GREATER_EQUAL "6.8")
+    set(QML_SEARCH_ACCESSIBILITY_STATUS_HANDLER [=[
+        function onAccessibilityStatusChanged() {
+            if (root.searchModel.accessibilityStatus.length > 0)
+                Accessible.announce(root.searchModel.accessibilityStatus, Accessible.Polite)
+        }
+]=])
+else()
+    set(QML_SEARCH_ACCESSIBILITY_STATUS_HANDLER "")
+endif()
+configure_file(SearchWindow.qml.in ${CMAKE_CURRENT_SOURCE_DIR}/SearchWindow.qml)
+
 target_sources(nextcloudGuiSearch
   PRIVATE
     unifiedsearchresult.h
@@ -13,10 +31,34 @@ target_sources(nextcloudGuiSearch
     unifiedsearchpeoplemodel.cpp
 )
 
-ecm_add_qml_module(nextcloudGuiSearch
-  URI com.nextcloud.desktopclient.search
-  GENERATE_PLUGIN_SOURCE
-  QML_FILES
+if(ECM_VERSION VERSION_GREATER_EQUAL "6.0.0")
+  ecm_add_qml_module(nextcloudGuiSearch
+    URI com.nextcloud.desktopclient.search
+    GENERATE_PLUGIN_SOURCE
+    QML_FILES
+      SearchWindow.qml
+      UnifiedSearchInputContainer.qml
+      UnifiedSearchResultFetchMoreTrigger.qml
+      UnifiedSearchResultItem.qml
+      UnifiedSearchResultItemSkeleton.qml
+      UnifiedSearchResultItemSkeletonContainer.qml
+      UnifiedSearchResultItemSkeletonGradientRectangle.qml
+      UnifiedSearchResultListItem.qml
+      UnifiedSearchResultNothingFound.qml
+      UnifiedSearchPlaceholderView.qml
+      UnifiedSearchResultSectionItem.qml
+  )
+else()
+  # Workaround for CMake AUTOMOC bug with static libraries on Qt 6.2.x.
+  # AUTOMOC doesn't properly propagate moc symbols from static libraries.
+  # See: https://gitlab.kitware.com/cmake/cmake/-/issues/19939
+  if(QT_VERSION_MAJOR STREQUAL "6" AND QT_VERSION_MINOR LESS 5)
+    qt_generate_moc(unifiedsearchresultslistmodel.h ${CMAKE_CURRENT_BINARY_DIR}/moc_unifiedsearchresultslistmodel.cpp)
+    target_sources(nextcloudGuiSearch PRIVATE ${CMAKE_CURRENT_BINARY_DIR}/moc_unifiedsearchresultslistmodel.cpp)
+    target_include_directories(nextcloudGuiSearch PUBLIC ${CMAKE_CURRENT_BINARY_DIR})
+  endif()
+
+  set(_search_qml_files
     SearchWindow.qml
     UnifiedSearchCustomDateRangeDialog.qml
     UnifiedSearchDetailHeader.qml
@@ -34,7 +76,74 @@ ecm_add_qml_module(nextcloudGuiSearch
     UnifiedSearchResultNothingFound.qml
     UnifiedSearchPlaceholderView.qml
     UnifiedSearchResultSectionItem.qml
-)
+  )
+
+  qt_add_resources(nextcloudGuiSearch search_qml
+    PREFIX "/qt/qml/com/nextcloud/desktopclient/search"
+    BASE "${CMAKE_CURRENT_SOURCE_DIR}"
+    FILES ${_search_qml_files}
+  )
+
+  set(_search_qmldir "${CMAKE_CURRENT_BINARY_DIR}/qmldir")
+  set(_search_qmldir_content "module com.nextcloud.desktopclient.search\n")
+  string(APPEND _search_qmldir_content "optional plugin nextcloudGuiSearchplugin\n")
+  string(APPEND _search_qmldir_content "classname com_nextcloud_desktopclient_searchPlugin\n")
+  string(APPEND _search_qmldir_content "prefer :/qt/qml/com/nextcloud/desktopclient/search/\n")
+  foreach(_search_qml_file IN LISTS _search_qml_files)
+    get_filename_component(_search_qml_type "${_search_qml_file}" NAME_WE)
+    string(APPEND _search_qmldir_content "${_search_qml_type} 254.0 ${_search_qml_file}\n")
+  endforeach()
+  file(WRITE "${_search_qmldir}" "${_search_qmldir_content}")
+  set_source_files_properties("${_search_qmldir}" PROPERTIES QT_RESOURCE_ALIAS "qmldir")
+  qt_add_resources(nextcloudGuiSearch search_qmldir
+    PREFIX "/qt/qml/com/nextcloud/desktopclient/search"
+    FILES "${_search_qmldir}"
+  )
+
+  file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/nextcloudGuiSearchplugin.cpp"
+    "#include <QQmlEngineExtensionPlugin>\n"
+    "#include <QtQml/qqmlmoduleregistration.h>\n"
+    "#include <qqml.h>\n"
+    "\n"
+    "#include \"gui/search/unifiedsearchpeoplemodel.h\"\n"
+    "#include \"gui/search/unifiedsearchresultslistmodel.h\"\n"
+    "\n"
+    "static void qml_register_types_com_nextcloud_desktopclient_search()\n"
+    "{\n"
+    "    qmlRegisterModule(\"com.nextcloud.desktopclient.search\", 1, 0);\n"
+    "    qmlRegisterType<OCC::UnifiedSearchPeopleModel>(\"com.nextcloud.desktopclient.search\", 1, 0, \"UnifiedSearchPeopleModel\");\n"
+    "    qmlRegisterType<OCC::UnifiedSearchResultsListModel>(\"com.nextcloud.desktopclient.search\", 1, 0, \"UnifiedSearchResultsListModel\");\n"
+    "}\n"
+    "static const QQmlModuleRegistration registration(\"com.nextcloud.desktopclient.search\", qml_register_types_com_nextcloud_desktopclient_search);\n"
+    "\n"
+    "class com_nextcloud_desktopclient_searchPlugin : public QQmlEngineExtensionPlugin\n"
+    "{\n"
+    "    Q_OBJECT\n"
+    "    Q_PLUGIN_METADATA(IID \"org.qt-project.Qt.QQmlEngineExtensionInterface\")\n"
+    "};\n"
+    "\n"
+    "#include \"nextcloudGuiSearchplugin.moc\"\n"
+  )
+  add_library(nextcloudGuiSearchplugin STATIC)
+  target_sources(nextcloudGuiSearchplugin PRIVATE
+    "${CMAKE_CURRENT_BINARY_DIR}/nextcloudGuiSearchplugin.cpp"
+  )
+  target_link_libraries(nextcloudGuiSearchplugin
+    PUBLIC
+      nextcloudGuiSearch
+    PRIVATE
+      nextcloudCore
+      Qt::Qml
+  )
+  set_target_properties(nextcloudGuiSearchplugin PROPERTIES
+    AUTOMOC ON
+    AUTOMOC_MOC_OPTIONS "-Muri=com.nextcloud.desktopclient.search"
+  )
+  # QT_STATICPLUGIN is required so that the moc-generated QT_MOC_EXPORT_PLUGIN
+  # code emits the qt_static_plugin_*() function referenced by Q_IMPORT_QML_PLUGIN
+  # in main.cpp.
+  target_compile_definitions(nextcloudGuiSearchplugin PUBLIC QT_STATICPLUGIN)
+endif()
 
 target_link_libraries(nextcloudGuiSearch PRIVATE nextcloudCore)
 target_compile_definitions(nextcloudGuiSearch PRIVATE QT_NO_KEYWORDS)
diff --git a/src/gui/search/SearchWindow.qml b/src/gui/search/SearchWindow.qml.in
similarity index 98%
rename from src/gui/search/SearchWindow.qml
rename to src/gui/search/SearchWindow.qml.in
index 7c88060069..f42fd7cbc3 100644
--- a/src/gui/search/SearchWindow.qml
+++ b/src/gui/search/SearchWindow.qml.in
@@ -87,10 +87,7 @@ WizardStyledWindow {
                 }
             })
         }
-        function onAccessibilityStatusChanged() {
-            if (root.searchModel.accessibilityStatus.length > 0)
-                Accessible.announce(root.searchModel.accessibilityStatus, Accessible.Polite)
-        }
+@QML_SEARCH_ACCESSIBILITY_STATUS_HANDLER@
     }
 
     Component.onCompleted: Qt.callLater(focusSearchInput)
diff --git a/src/gui/search/UnifiedSearchFilterBar.qml b/src/gui/search/UnifiedSearchFilterBar.qml.in
similarity index 99%
rename from src/gui/search/UnifiedSearchFilterBar.qml
rename to src/gui/search/UnifiedSearchFilterBar.qml.in
index b21939dc59..20259dd6c7 100644
--- a/src/gui/search/UnifiedSearchFilterBar.qml
+++ b/src/gui/search/UnifiedSearchFilterBar.qml.in
@@ -3,7 +3,7 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
-pragma ComponentBehavior: Bound
+@QML_COMPONENT_BEHAVIOR_PRAGMA@
 
 import QtQuick
 import QtQuick.Controls.Basic
diff --git a/src/gui/search/UnifiedSearchInputContainer.qml b/src/gui/search/UnifiedSearchInputContainer.qml
index 57dcb0519f..29ba169c1f 100644
--- a/src/gui/search/UnifiedSearchInputContainer.qml
+++ b/src/gui/search/UnifiedSearchInputContainer.qml
@@ -7,6 +7,7 @@ import QtQuick
 import QtQuick.Controls.Basic
 import Style
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.search
 import "qrc:/qml/src/gui/tray"
 
 TextField {
diff --git a/src/gui/search/UnifiedSearchPeoplePopup.qml b/src/gui/search/UnifiedSearchPeoplePopup.qml.in
similarity index 98%
rename from src/gui/search/UnifiedSearchPeoplePopup.qml
rename to src/gui/search/UnifiedSearchPeoplePopup.qml.in
index 49dcc3e3d5..ab244ae635 100644
--- a/src/gui/search/UnifiedSearchPeoplePopup.qml
+++ b/src/gui/search/UnifiedSearchPeoplePopup.qml.in
@@ -3,7 +3,7 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
-pragma ComponentBehavior: Bound
+@QML_COMPONENT_BEHAVIOR_PRAGMA@
 
 import QtQuick
 import QtQuick.Controls.Basic
diff --git a/src/gui/search/UnifiedSearchResultDelegate.qml b/src/gui/search/UnifiedSearchResultDelegate.qml
index 905480d293..6e6451d620 100644
--- a/src/gui/search/UnifiedSearchResultDelegate.qml
+++ b/src/gui/search/UnifiedSearchResultDelegate.qml
@@ -9,6 +9,7 @@ import QtQuick.Layouts
 
 import Style
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.search
 import "qrc:/qml/src/gui/tray"
 
 Item {
diff --git a/src/gui/search/UnifiedSearchResultListItem.qml b/src/gui/search/UnifiedSearchResultListItem.qml
index 6c998598fb..5866e1615a 100644
--- a/src/gui/search/UnifiedSearchResultListItem.qml
+++ b/src/gui/search/UnifiedSearchResultListItem.qml
@@ -30,9 +30,17 @@ MouseArea {
     height: Style.unifiedSearchItemHeight
 
     ToolTip {
-        popupType: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+        id: unifiedSearchResultMouseAreaToolTop
+
         visible: unifiedSearchResultMouseArea.containsMouse
         text: isFetchMoreTrigger ? qsTr("Load more results") : model.resultTitle + "\n\n" + model.subline
+
+        Binding {
+            target: unifiedSearchResultMouseAreaToolTop
+            property: "popupType"
+            value: Qt.platform.os === "windows" ? Popup.Item : Popup.Native
+            when: unifiedSearchResultMouseAreaToolTop.hasOwnProperty("popupType")
+        }
     }
 
     Rectangle {
diff --git a/src/gui/search/unifiedsearchpeoplemodel.cpp b/src/gui/search/unifiedsearchpeoplemodel.cpp
index 69257c11c7..6543560600 100644
--- a/src/gui/search/unifiedsearchpeoplemodel.cpp
+++ b/src/gui/search/unifiedsearchpeoplemodel.cpp
@@ -5,6 +5,8 @@
 
 #include "unifiedsearchpeoplemodel.h"
 
+#include "common/qtcompat.h"
+
 #include "account.h"
 #include "accountstate.h"
 #include "common/utility.h"
diff --git a/src/gui/search/unifiedsearchpeoplemodel.h b/src/gui/search/unifiedsearchpeoplemodel.h
index 871f27bb9a..4344bdf66b 100644
--- a/src/gui/search/unifiedsearchpeoplemodel.h
+++ b/src/gui/search/unifiedsearchpeoplemodel.h
@@ -5,12 +5,14 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
+
 #include "accountstate.h"
 
 #include <QAbstractListModel>
 #include <QPointer>
 #include <QTimer>
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 namespace OCC {
 
diff --git a/src/gui/search/unifiedsearchresultslistmodel.cpp b/src/gui/search/unifiedsearchresultslistmodel.cpp
index 34934e66e7..dc22d8a0cc 100644
--- a/src/gui/search/unifiedsearchresultslistmodel.cpp
+++ b/src/gui/search/unifiedsearchresultslistmodel.cpp
@@ -11,6 +11,7 @@
 #include "guiutility.h"
 #include "networkjobs.h"
 #include "tray/usermodel.h"
+#include "common/qtcompat.h"
 
 #include <QDesktopServices>
 #include <QFileInfo>
diff --git a/src/gui/search/unifiedsearchresultslistmodel.h b/src/gui/search/unifiedsearchresultslistmodel.h
index 361953572e..972c9ac746 100644
--- a/src/gui/search/unifiedsearchresultslistmodel.h
+++ b/src/gui/search/unifiedsearchresultslistmodel.h
@@ -5,6 +5,8 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
+
 #include <QAbstractListModel>
 #include <QDateTime>
 #include <QJsonArray>
@@ -15,14 +17,14 @@
 #include <QtCore>
 #include <QTimer>
 #include <QUrlQuery>
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 #include <limits>
 
+#include "accountstate.h"
 #include "unifiedsearchresult.h"
 
 namespace OCC {
-class AccountState;
 
 /**
  * @brief Account-scoped presentation model for Nextcloud Unified Search.
diff --git a/src/gui/settingsdialog.cpp b/src/gui/settingsdialog.cpp
index 0291dc8f1e..aa1454a421 100644
--- a/src/gui/settingsdialog.cpp
+++ b/src/gui/settingsdialog.cpp
@@ -17,6 +17,7 @@
 #include "progressdispatcher.h"
 #include "owncloudgui.h"
 #include "accountmanager.h"
+#include "common/qtcompat.h"
 
 #include <QLabel>
 #include <QStandardItemModel>
diff --git a/src/gui/sharemanager.cpp b/src/gui/sharemanager.cpp
index a57349d15e..5b57d47e8f 100644
--- a/src/gui/sharemanager.cpp
+++ b/src/gui/sharemanager.cpp
@@ -11,6 +11,7 @@
 #include "accountstate.h"
 #include "clientsideencryption.h"
 #include "updatee2eefolderusersmetadatajob.h"
+#include "common/qtcompat.h"
 
 #include <QUrl>
 #include <QJsonDocument>
diff --git a/src/gui/sharing/CMakeLists.txt b/src/gui/sharing/CMakeLists.txt
index 3e82ce426a..19f120f052 100644
--- a/src/gui/sharing/CMakeLists.txt
+++ b/src/gui/sharing/CMakeLists.txt
@@ -1,8 +1,40 @@
 # SPDX-FileCopyrightText: 2026 Nextcloud GmbH and Nextcloud contributors
 # SPDX-License-Identifier: GPL-2.0-or-later
 
+set(CMAKE_AUTOMOC ON)
+
 add_library(nextcloudGuiSharing STATIC)
 
+if ("${QT_VERSION_MAJOR}.${QT_VERSION_MINOR}" VERSION_GREATER_EQUAL "6.5")
+    set(QML_AVATAR_EFFECT_IMPORT "import QtQuick.Effects")
+    set(QML_AVATAR_EFFECT [=[
+    MultiEffect {
+        objectName: "recipientAvatarEffect"
+
+        anchors.fill: parent
+        source: avatarImage
+        maskEnabled: true
+        maskSource: maskShape
+    }
+]=])
+else()
+    set(QML_AVATAR_EFFECT_IMPORT "import Qt5Compat.GraphicalEffects")
+    set(QML_AVATAR_EFFECT [=[
+    OpacityMask {
+        objectName: "recipientAvatarEffect"
+
+        anchors.fill: parent
+        source: avatarImage
+        maskSource: maskShape
+    }
+]=])
+endif()
+configure_file(RecipientAvatar.qml.in ${CMAKE_CURRENT_SOURCE_DIR}/RecipientAvatar.qml)
+
+foreach(_sharing_component_behavior_file FieldDelegate.qml ShareAdvancedSettingsPage.qml ShareDetailsFrame.qml ShareDialog.qml ShareListPage.qml)
+  configure_file(${_sharing_component_behavior_file}.in ${CMAKE_CURRENT_SOURCE_DIR}/${_sharing_component_behavior_file})
+endforeach()
+
 set_source_files_properties(RecipientIcon.qml PROPERTIES QT_QML_SINGLETON_TYPE TRUE)
 
 target_sources(nextcloudGuiSharing
@@ -70,10 +102,29 @@ target_sources(nextcloudGuiSharing
     unifiedsharelistmodel.cpp
 )
 
-ecm_add_qml_module(nextcloudGuiSharing
-  URI com.nextcloud.desktopclient.sharing
-  GENERATE_PLUGIN_SOURCE
-  QML_FILES
+if(ECM_VERSION VERSION_GREATER_EQUAL "6.0.0")
+  ecm_add_qml_module(nextcloudGuiSharing
+    URI com.nextcloud.desktopclient.sharing
+    GENERATE_PLUGIN_SOURCE
+    QML_FILES
+      FieldDelegate.qml
+      PermissionList.qml
+      RecipientPermissionDialog.qml
+      DeleteShareConfirmation.qml
+      RecipientAvatar.qml
+      RecipientIcon.qml
+      RecipientSearchField.qml
+      ShareActionRow.qml
+      ShareDetailsPage.qml
+      ShareAdvancedSettingsPage.qml
+      ShareDetailsFrame.qml
+      ShareListPage.qml
+      ShareRow.qml
+      SettingsPanel.qml
+      ShareDialog.qml
+  )
+else()
+  set(_sharing_qml_files
     FieldDelegate.qml
     PermissionList.qml
     RecipientPermissionDialog.qml
@@ -89,6 +140,91 @@ ecm_add_qml_module(nextcloudGuiSharing
     ShareRow.qml
     SettingsPanel.qml
     ShareDialog.qml
-)
+  )
+
+  qt_add_resources(nextcloudGuiSharing sharing_qml
+    PREFIX "/qt/qml/com/nextcloud/desktopclient/sharing"
+    BASE "${CMAKE_CURRENT_SOURCE_DIR}"
+    FILES ${_sharing_qml_files}
+  )
+
+  set(_sharing_qmldir "${CMAKE_CURRENT_BINARY_DIR}/qmldir")
+  set(_sharing_qmldir_content "module com.nextcloud.desktopclient.sharing\n")
+  string(APPEND _sharing_qmldir_content "optional plugin nextcloudGuiSharingplugin\n")
+  string(APPEND _sharing_qmldir_content "classname com_nextcloud_desktopclient_sharingPlugin\n")
+  string(APPEND _sharing_qmldir_content "prefer :/qt/qml/com/nextcloud/desktopclient/sharing/\n")
+  foreach(_sharing_qml_file IN LISTS _sharing_qml_files)
+    get_filename_component(_sharing_qml_type "${_sharing_qml_file}" NAME_WE)
+    if(_sharing_qml_file STREQUAL "RecipientIcon.qml")
+      string(APPEND _sharing_qmldir_content "singleton ${_sharing_qml_type} 254.0 ${_sharing_qml_file}\n")
+    else()
+      string(APPEND _sharing_qmldir_content "${_sharing_qml_type} 254.0 ${_sharing_qml_file}\n")
+    endif()
+  endforeach()
+  file(WRITE "${_sharing_qmldir}" "${_sharing_qmldir_content}")
+  set_source_files_properties("${_sharing_qmldir}" PROPERTIES QT_RESOURCE_ALIAS "qmldir")
+  qt_add_resources(nextcloudGuiSharing sharing_qmldir
+    PREFIX "/qt/qml/com/nextcloud/desktopclient/sharing"
+    FILES "${_sharing_qmldir}"
+  )
+
+  file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/nextcloudGuiSharingplugin.cpp"
+    "#include <QQmlEngineExtensionPlugin>\n"
+    "#include <QtQml/qqmlmoduleregistration.h>\n"
+    "#include <qqml.h>\n"
+    "\n"
+    "#include \"gui/sharing/permissionmodel.h\"\n"
+    "#include \"gui/sharing/propertymodel.h\"\n"
+    "#include \"gui/sharing/recipient.h\"\n"
+    "#include \"gui/sharing/recipientmodel.h\"\n"
+    "#include \"gui/sharing/recipientsearchmodel.h\"\n"
+    "#include \"gui/sharing/sharingcontroller.h\"\n"
+    "#include \"gui/sharing/unifiedshare.h\"\n"
+    "#include \"gui/sharing/unifiedsharelistmodel.h\"\n"
+    "\n"
+    "static void qml_register_types_com_nextcloud_desktopclient_sharing()\n"
+    "{\n"
+    "    qmlRegisterModule(\"com.nextcloud.desktopclient.sharing\", 1, 0);\n"
+    "    qmlRegisterType<OCC::Gui::Sharing::PermissionModel>(\"com.nextcloud.desktopclient.sharing\", 1, 0, \"PermissionModel\");\n"
+    "    qmlRegisterType<OCC::Gui::Sharing::PropertyModel>(\"com.nextcloud.desktopclient.sharing\", 1, 0, \"PropertyModel\");\n"
+    "    qmlRegisterUncreatableType<OCC::Gui::Sharing::Recipient>(\"com.nextcloud.desktopclient.sharing\", 1, 0, \"Recipient\", QStringLiteral(\"created by SharingController\"));\n"
+    "    qmlRegisterType<OCC::Gui::Sharing::RecipientModel>(\"com.nextcloud.desktopclient.sharing\", 1, 0, \"RecipientModel\");\n"
+    "    qmlRegisterType<OCC::Gui::Sharing::RecipientSearchModel>(\"com.nextcloud.desktopclient.sharing\", 1, 0, \"RecipientSearchModel\");\n"
+    "    qmlRegisterUncreatableType<OCC::Gui::Sharing::Share>(\"com.nextcloud.desktopclient.sharing\", 1, 0, \"Share\", QStringLiteral(\"created via SharingController\"));\n"
+    "    qmlRegisterType<OCC::Gui::Sharing::SharingController>(\"com.nextcloud.desktopclient.sharing\", 1, 0, \"SharingController\");\n"
+    "    qmlRegisterType<OCC::Gui::Sharing::UnifiedShareListModel>(\"com.nextcloud.desktopclient.sharing\", 1, 0, \"UnifiedShareListModel\");\n"
+    "}\n"
+    "static const QQmlModuleRegistration registration(\"com.nextcloud.desktopclient.sharing\", qml_register_types_com_nextcloud_desktopclient_sharing);\n"
+    "\n"
+    "class com_nextcloud_desktopclient_sharingPlugin : public QQmlEngineExtensionPlugin\n"
+    "{\n"
+    "    Q_OBJECT\n"
+    "    Q_PLUGIN_METADATA(IID \"org.qt-project.Qt.QQmlEngineExtensionInterface\")\n"
+    "};\n"
+    "\n"
+    "#include \"nextcloudGuiSharingplugin.moc\"\n"
+  )
+  add_library(nextcloudGuiSharingplugin STATIC)
+  target_sources(nextcloudGuiSharingplugin PRIVATE
+    "${CMAKE_CURRENT_BINARY_DIR}/nextcloudGuiSharingplugin.cpp"
+  )
+  target_link_libraries(nextcloudGuiSharingplugin
+    PUBLIC
+      nextcloudGuiSharing
+    PRIVATE
+      nextcloudCore
+      Qt::Qml
+  )
+  set_target_properties(nextcloudGuiSharingplugin PROPERTIES
+    AUTOMOC ON
+    AUTOMOC_MOC_OPTIONS "-Muri=com.nextcloud.desktopclient.sharing"
+  )
+  # QT_STATICPLUGIN is required so that the moc-generated QT_MOC_EXPORT_PLUGIN
+  # code emits the qt_static_plugin_*() function referenced by Q_IMPORT_QML_PLUGIN
+  # in main.cpp.
+  target_compile_definitions(nextcloudGuiSharingplugin PUBLIC QT_STATICPLUGIN)
+endif()
 
 target_link_libraries(nextcloudGuiSharing PRIVATE nextcloudCore)
+target_compile_definitions(nextcloudGuiSharing PRIVATE QT_NO_KEYWORDS)
+target_compile_definitions(nextcloudGuiSharingplugin PRIVATE QT_NO_KEYWORDS)
diff --git a/src/gui/sharing/FieldDelegate.qml b/src/gui/sharing/FieldDelegate.qml.in
similarity index 99%
rename from src/gui/sharing/FieldDelegate.qml
rename to src/gui/sharing/FieldDelegate.qml.in
index 681917622f..1493bd8635 100644
--- a/src/gui/sharing/FieldDelegate.qml
+++ b/src/gui/sharing/FieldDelegate.qml.in
@@ -3,7 +3,7 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
-pragma ComponentBehavior: Bound
+@QML_COMPONENT_BEHAVIOR_PRAGMA@
 
 import QtQuick
 import QtQuick.Window
@@ -11,6 +11,7 @@ import QtQuick.Layouts
 import QtQuick.Controls
 
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui/tray"
 import "qrc:/qml/src/gui/wizard/qml"
diff --git a/src/gui/sharing/RecipientAvatar.qml b/src/gui/sharing/RecipientAvatar.qml.in
similarity index 77%
rename from src/gui/sharing/RecipientAvatar.qml
rename to src/gui/sharing/RecipientAvatar.qml.in
index 6e8e4ddafc..4dfa85b316 100644
--- a/src/gui/sharing/RecipientAvatar.qml
+++ b/src/gui/sharing/RecipientAvatar.qml.in
@@ -4,7 +4,7 @@
  */
 
 import QtQuick
-import QtQuick.Effects
+@QML_AVATAR_EFFECT_IMPORT@
 
 Item {
     id: root
@@ -34,12 +34,5 @@ Item {
         layer.enabled: true
     }
 
-    MultiEffect {
-        objectName: "recipientAvatarEffect"
-
-        anchors.fill: parent
-        source: avatarImage
-        maskEnabled: true
-        maskSource: maskShape
-    }
+@QML_AVATAR_EFFECT@
 }
diff --git a/src/gui/sharing/RecipientPermissionDialog.qml b/src/gui/sharing/RecipientPermissionDialog.qml
index aff4f8e5f8..e3c470b89c 100644
--- a/src/gui/sharing/RecipientPermissionDialog.qml
+++ b/src/gui/sharing/RecipientPermissionDialog.qml
@@ -8,6 +8,7 @@ import QtQuick.Layouts
 import QtQuick.Controls
 
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui"
 import "qrc:/qml/src/gui/tray"
diff --git a/src/gui/sharing/RecipientSearchField.qml b/src/gui/sharing/RecipientSearchField.qml
index b372f815e4..fefe46a304 100644
--- a/src/gui/sharing/RecipientSearchField.qml
+++ b/src/gui/sharing/RecipientSearchField.qml
@@ -10,6 +10,7 @@ import QtQuick.Controls
 
 import com.nextcloud.desktopclient
 import com.nextcloud.desktopclient as NC
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui/tray"
 import "qrc:/qml/src/gui/wizard/qml"
diff --git a/src/gui/sharing/ShareAdvancedSettingsPage.qml b/src/gui/sharing/ShareAdvancedSettingsPage.qml.in
similarity index 96%
rename from src/gui/sharing/ShareAdvancedSettingsPage.qml
rename to src/gui/sharing/ShareAdvancedSettingsPage.qml.in
index eb1fc2d135..a7f7e7f819 100644
--- a/src/gui/sharing/ShareAdvancedSettingsPage.qml
+++ b/src/gui/sharing/ShareAdvancedSettingsPage.qml.in
@@ -3,13 +3,14 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
-pragma ComponentBehavior: Bound
+@QML_COMPONENT_BEHAVIOR_PRAGMA@
 
 import QtQuick
 import QtQuick.Layouts
 import QtQuick.Controls
 
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui"
 import "qrc:/qml/src/gui/tray"
diff --git a/src/gui/sharing/ShareDetailsFrame.qml b/src/gui/sharing/ShareDetailsFrame.qml.in
similarity index 98%
rename from src/gui/sharing/ShareDetailsFrame.qml
rename to src/gui/sharing/ShareDetailsFrame.qml.in
index 229338a052..871bf930dd 100644
--- a/src/gui/sharing/ShareDetailsFrame.qml
+++ b/src/gui/sharing/ShareDetailsFrame.qml.in
@@ -3,13 +3,14 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
-pragma ComponentBehavior: Bound
+@QML_COMPONENT_BEHAVIOR_PRAGMA@
 
 import QtQuick
 import QtQuick.Layouts
 import QtQuick.Controls
 
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui"
 import "qrc:/qml/src/gui/tray"
diff --git a/src/gui/sharing/ShareDetailsPage.qml b/src/gui/sharing/ShareDetailsPage.qml
index 9798c2f1bb..634a8b8033 100644
--- a/src/gui/sharing/ShareDetailsPage.qml
+++ b/src/gui/sharing/ShareDetailsPage.qml
@@ -8,6 +8,7 @@ import QtQuick.Layouts
 import QtQuick.Controls
 
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui"
 import "qrc:/qml/src/gui/tray"
diff --git a/src/gui/sharing/ShareDialog.qml b/src/gui/sharing/ShareDialog.qml.in
similarity index 99%
rename from src/gui/sharing/ShareDialog.qml
rename to src/gui/sharing/ShareDialog.qml.in
index eac1d249a8..1c21f544c8 100644
--- a/src/gui/sharing/ShareDialog.qml
+++ b/src/gui/sharing/ShareDialog.qml.in
@@ -3,7 +3,7 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
-pragma ComponentBehavior: Bound
+@QML_COMPONENT_BEHAVIOR_PRAGMA@
 
 import QtQuick
 import QtQuick.Window
@@ -11,6 +11,7 @@ import QtQuick.Layouts
 import QtQuick.Controls
 
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui"
 import "qrc:/qml/src/gui/tray"
diff --git a/src/gui/sharing/ShareListPage.qml b/src/gui/sharing/ShareListPage.qml.in
similarity index 98%
rename from src/gui/sharing/ShareListPage.qml
rename to src/gui/sharing/ShareListPage.qml.in
index 47f884804d..548b6aeaea 100644
--- a/src/gui/sharing/ShareListPage.qml
+++ b/src/gui/sharing/ShareListPage.qml.in
@@ -3,13 +3,14 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
-pragma ComponentBehavior: Bound
+@QML_COMPONENT_BEHAVIOR_PRAGMA@
 
 import QtQuick
 import QtQuick.Layouts
 import QtQuick.Controls
 
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui"
 import "qrc:/qml/src/gui/tray"
diff --git a/src/gui/sharing/ShareRow.qml b/src/gui/sharing/ShareRow.qml
index b39910fdc6..ab086112c0 100644
--- a/src/gui/sharing/ShareRow.qml
+++ b/src/gui/sharing/ShareRow.qml
@@ -8,6 +8,7 @@ import QtQuick.Layouts
 import QtQuick.Controls
 
 import com.nextcloud.desktopclient
+import com.nextcloud.desktopclient.sharing
 import Style
 import "qrc:/qml/src/gui/tray"
 import "qrc:/qml/src/gui/wizard/qml"
diff --git a/src/gui/sharing/addrecipientjob.cpp b/src/gui/sharing/addrecipientjob.cpp
index e58a20eed8..40d4e99bee 100644
--- a/src/gui/sharing/addrecipientjob.cpp
+++ b/src/gui/sharing/addrecipientjob.cpp
@@ -5,6 +5,8 @@
 
 #include "addrecipientjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/addsourcejob.cpp b/src/gui/sharing/addsourcejob.cpp
index 11d0b70477..47ea908b46 100644
--- a/src/gui/sharing/addsourcejob.cpp
+++ b/src/gui/sharing/addsourcejob.cpp
@@ -5,6 +5,7 @@
 
 #include "addsourcejob.h"
 
+#include "common/qtcompat.h"
 #include "sharingconstants.h"
 
 using namespace Qt::StringLiterals;
diff --git a/src/gui/sharing/createsharejob.cpp b/src/gui/sharing/createsharejob.cpp
index 8457d87f20..0aef0b0d26 100644
--- a/src/gui/sharing/createsharejob.cpp
+++ b/src/gui/sharing/createsharejob.cpp
@@ -5,6 +5,8 @@
 
 #include "createsharejob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/deletesharejob.cpp b/src/gui/sharing/deletesharejob.cpp
index 3fb7d7085a..fdf9113519 100644
--- a/src/gui/sharing/deletesharejob.cpp
+++ b/src/gui/sharing/deletesharejob.cpp
@@ -5,6 +5,8 @@
 
 #include "deletesharejob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/generatesecretjob.cpp b/src/gui/sharing/generatesecretjob.cpp
index 18ae7e1f52..455e6b064b 100644
--- a/src/gui/sharing/generatesecretjob.cpp
+++ b/src/gui/sharing/generatesecretjob.cpp
@@ -5,6 +5,8 @@
 
 #include "generatesecretjob.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonDocument>
 #include <QJsonObject>
 
diff --git a/src/gui/sharing/getsharejob.cpp b/src/gui/sharing/getsharejob.cpp
index a12d761e49..9c711735fb 100644
--- a/src/gui/sharing/getsharejob.cpp
+++ b/src/gui/sharing/getsharejob.cpp
@@ -5,6 +5,8 @@
 
 #include "getsharejob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/getsharesjob.cpp b/src/gui/sharing/getsharesjob.cpp
index f757c9bc47..772ecd2e84 100644
--- a/src/gui/sharing/getsharesjob.cpp
+++ b/src/gui/sharing/getsharesjob.cpp
@@ -5,6 +5,8 @@
 
 #include "getsharesjob.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonDocument>
 
 using namespace Qt::StringLiterals;
diff --git a/src/gui/sharing/permission.cpp b/src/gui/sharing/permission.cpp
index 5665e592d7..2bc86a7ebf 100644
--- a/src/gui/sharing/permission.cpp
+++ b/src/gui/sharing/permission.cpp
@@ -6,6 +6,8 @@
 
 #include "permission.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonObject>
 
 using namespace Qt::StringLiterals;
diff --git a/src/gui/sharing/permissionmodel.cpp b/src/gui/sharing/permissionmodel.cpp
index 8d1321d7ec..99e6a75c3d 100644
--- a/src/gui/sharing/permissionmodel.cpp
+++ b/src/gui/sharing/permissionmodel.cpp
@@ -5,6 +5,7 @@
 
 #include "permissionmodel.h"
 
+#include "common/qtcompat.h"
 #include "permission.h"
 #include "recipient.h"
 #include "unifiedshare.h"
diff --git a/src/gui/sharing/permissionmodel.h b/src/gui/sharing/permissionmodel.h
index 0c1b8a7f34..20b8c276c6 100644
--- a/src/gui/sharing/permissionmodel.h
+++ b/src/gui/sharing/permissionmodel.h
@@ -5,9 +5,10 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
 #include "sharedetailslistmodel.h"
 
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 namespace OCC::Gui::Sharing {
 
diff --git a/src/gui/sharing/property.cpp b/src/gui/sharing/property.cpp
index 20d1d860d0..ff58c8c9a4 100644
--- a/src/gui/sharing/property.cpp
+++ b/src/gui/sharing/property.cpp
@@ -6,6 +6,8 @@
 
 #include "property.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonArray>
 #include <QJsonObject>
 
diff --git a/src/gui/sharing/propertymodel.cpp b/src/gui/sharing/propertymodel.cpp
index 05a041e6b4..71db7db630 100644
--- a/src/gui/sharing/propertymodel.cpp
+++ b/src/gui/sharing/propertymodel.cpp
@@ -5,6 +5,7 @@
 
 #include "propertymodel.h"
 
+#include "common/qtcompat.h"
 #include "unifiedshare.h"
 #include "property.h"
 
diff --git a/src/gui/sharing/propertymodel.h b/src/gui/sharing/propertymodel.h
index cf06c93591..95ede1f7eb 100644
--- a/src/gui/sharing/propertymodel.h
+++ b/src/gui/sharing/propertymodel.h
@@ -5,10 +5,11 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
 #include "sharedetailslistmodel.h"
 #include "property.h"
 
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 namespace OCC::Gui::Sharing {
 
diff --git a/src/gui/sharing/recipient.cpp b/src/gui/sharing/recipient.cpp
index 60ea7820a5..98ada8d639 100644
--- a/src/gui/sharing/recipient.cpp
+++ b/src/gui/sharing/recipient.cpp
@@ -5,6 +5,8 @@
 
 #include "recipient.h"
 
+#include "common/qtcompat.h"
+
 #include <algorithm>
 
 #include <QJsonArray>
diff --git a/src/gui/sharing/recipient.h b/src/gui/sharing/recipient.h
index 55b51f0c35..eb1d0a4cfd 100644
--- a/src/gui/sharing/recipient.h
+++ b/src/gui/sharing/recipient.h
@@ -5,12 +5,14 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
+
 #include <QHash>
 #include <QJsonObject>
 #include <QList>
 #include <QObject>
 
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 #include <memory>
 #include <vector>
diff --git a/src/gui/sharing/recipienticonutils.cpp b/src/gui/sharing/recipienticonutils.cpp
index 35a71f59d6..fe284f1b30 100644
--- a/src/gui/sharing/recipienticonutils.cpp
+++ b/src/gui/sharing/recipienticonutils.cpp
@@ -5,6 +5,8 @@
 
 #include "recipienticonutils.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 QString OCC::Gui::Sharing::RecipientIconUtils::svgDataUrl(const QString &svg)
diff --git a/src/gui/sharing/recipientmodel.cpp b/src/gui/sharing/recipientmodel.cpp
index 2286699047..1793df0591 100644
--- a/src/gui/sharing/recipientmodel.cpp
+++ b/src/gui/sharing/recipientmodel.cpp
@@ -5,6 +5,7 @@
 
 #include "recipientmodel.h"
 
+#include "common/qtcompat.h"
 #include "recipient.h"
 #include "recipienticonutils.h"
 #include "unifiedshare.h"
diff --git a/src/gui/sharing/recipientmodel.h b/src/gui/sharing/recipientmodel.h
index 235d273337..ad9eff687d 100644
--- a/src/gui/sharing/recipientmodel.h
+++ b/src/gui/sharing/recipientmodel.h
@@ -5,9 +5,10 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
 #include "sharedetailslistmodel.h"
 
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 namespace OCC::Gui::Sharing
 {
diff --git a/src/gui/sharing/recipientsearchmodel.cpp b/src/gui/sharing/recipientsearchmodel.cpp
index df4046d7bb..c50e5db185 100644
--- a/src/gui/sharing/recipientsearchmodel.cpp
+++ b/src/gui/sharing/recipientsearchmodel.cpp
@@ -5,6 +5,8 @@
 
 #include "recipientsearchmodel.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonObject>
 #include <QLoggingCategory>
 
diff --git a/src/gui/sharing/recipientsearchmodel.h b/src/gui/sharing/recipientsearchmodel.h
index 19aa51230f..65d02f3d14 100644
--- a/src/gui/sharing/recipientsearchmodel.h
+++ b/src/gui/sharing/recipientsearchmodel.h
@@ -5,11 +5,13 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
+
 #include <QAbstractListModel>
 #include <QTimer>
 
 #include <QJsonArray>
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 #include "accountfwd.h"
 
@@ -21,6 +23,9 @@ class RecipientSearchModel : public QAbstractListModel
     Q_OBJECT
     QML_ELEMENT
 
+    // The moc-generated qt_metaTypeArray requires the full type definition of
+    // OCC::Account to instantiate the metatype for AccountPtr.
+    Q_MOC_INCLUDE("libsync/account.h")
     Q_PROPERTY(AccountPtr account READ account WRITE setAccount NOTIFY accountChanged)
     Q_PROPERTY(QString query READ query WRITE setQuery NOTIFY queryChanged)
     Q_PROPERTY(QString shareId READ shareId WRITE setShareId NOTIFY shareIdChanged)
diff --git a/src/gui/sharing/removerecipientjob.cpp b/src/gui/sharing/removerecipientjob.cpp
index 29877dae1b..069566b52b 100644
--- a/src/gui/sharing/removerecipientjob.cpp
+++ b/src/gui/sharing/removerecipientjob.cpp
@@ -5,6 +5,8 @@
 
 #include "removerecipientjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/removesourcejob.cpp b/src/gui/sharing/removesourcejob.cpp
index fd1370f0b2..c9d52bc20a 100644
--- a/src/gui/sharing/removesourcejob.cpp
+++ b/src/gui/sharing/removesourcejob.cpp
@@ -5,6 +5,7 @@
 
 #include "removesourcejob.h"
 
+#include "common/qtcompat.h"
 #include "sharingconstants.h"
 
 using namespace Qt::StringLiterals;
diff --git a/src/gui/sharing/searchrecipientsjob.cpp b/src/gui/sharing/searchrecipientsjob.cpp
index 608b3401ac..9487420547 100644
--- a/src/gui/sharing/searchrecipientsjob.cpp
+++ b/src/gui/sharing/searchrecipientsjob.cpp
@@ -5,6 +5,8 @@
 
 #include "searchrecipientsjob.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonDocument>
 #include <QJsonObject>
 
diff --git a/src/gui/sharing/setpermissionjob.cpp b/src/gui/sharing/setpermissionjob.cpp
index 40b863fa4a..94dee4d30d 100644
--- a/src/gui/sharing/setpermissionjob.cpp
+++ b/src/gui/sharing/setpermissionjob.cpp
@@ -5,6 +5,8 @@
 
 #include "setpermissionjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/setpermissionpresetjob.cpp b/src/gui/sharing/setpermissionpresetjob.cpp
index 603b6a6a20..c894968271 100644
--- a/src/gui/sharing/setpermissionpresetjob.cpp
+++ b/src/gui/sharing/setpermissionpresetjob.cpp
@@ -5,6 +5,8 @@
 
 #include "setpermissionpresetjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/setpropertyjob.cpp b/src/gui/sharing/setpropertyjob.cpp
index fc09bf52a5..47d031a01d 100644
--- a/src/gui/sharing/setpropertyjob.cpp
+++ b/src/gui/sharing/setpropertyjob.cpp
@@ -5,6 +5,8 @@
 
 #include "setpropertyjob.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonValue>
 
 using namespace Qt::StringLiterals;
diff --git a/src/gui/sharing/setrecipientpermissionjob.cpp b/src/gui/sharing/setrecipientpermissionjob.cpp
index b3e5d96e12..6334915212 100644
--- a/src/gui/sharing/setrecipientpermissionjob.cpp
+++ b/src/gui/sharing/setrecipientpermissionjob.cpp
@@ -5,6 +5,8 @@
 
 #include "setrecipientpermissionjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/setrecipientsecretjob.cpp b/src/gui/sharing/setrecipientsecretjob.cpp
index aa795d51c6..2ca0f42e3a 100644
--- a/src/gui/sharing/setrecipientsecretjob.cpp
+++ b/src/gui/sharing/setrecipientsecretjob.cpp
@@ -5,6 +5,8 @@
 
 #include "setrecipientsecretjob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/setsharestatejob.cpp b/src/gui/sharing/setsharestatejob.cpp
index d2bf7ec9a1..c468e5eafb 100644
--- a/src/gui/sharing/setsharestatejob.cpp
+++ b/src/gui/sharing/setsharestatejob.cpp
@@ -5,6 +5,8 @@
 
 #include "setsharestatejob.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC::Gui::Sharing
diff --git a/src/gui/sharing/sharedetailslistmodel.cpp b/src/gui/sharing/sharedetailslistmodel.cpp
index 80da517577..d098ff59cf 100644
--- a/src/gui/sharing/sharedetailslistmodel.cpp
+++ b/src/gui/sharing/sharedetailslistmodel.cpp
@@ -5,6 +5,7 @@
 
 #include "sharedetailslistmodel.h"
 
+#include "common/qtcompat.h"
 #include "unifiedshare.h"
 
 using namespace Qt::StringLiterals;
diff --git a/src/gui/sharing/sharingconstants.h b/src/gui/sharing/sharingconstants.h
index 3d29f46fbd..60d8e78fa4 100644
--- a/src/gui/sharing/sharingconstants.h
+++ b/src/gui/sharing/sharingconstants.h
@@ -5,22 +5,24 @@
 
 #pragma once
 
-#include <QLatin1StringView>
+#include "common/qtcompat.h"
+
+#include QLatin1StringViewOrStringHeader
 
 namespace OCC::Gui::Sharing::SourceTypeClasses
 {
 
-inline constexpr auto node = QLatin1StringView{"OCA\\Files\\Sharing\\Source\\NodeShareSourceType"};
+inline constexpr auto node = QLatin1StringOrView{"OCA\\Files\\Sharing\\Source\\NodeShareSourceType"};
 
 }
 
 namespace OCC::Gui::Sharing::RecipientTypeClasses
 {
 
-inline constexpr auto email = QLatin1StringView{"OC\\Core\\Sharing\\Recipient\\EmailShareRecipientType"};
-inline constexpr auto group = QLatin1StringView{"OC\\Core\\Sharing\\Recipient\\GroupShareRecipientType"};
-inline constexpr auto team = QLatin1StringView{"OC\\Core\\Sharing\\Recipient\\TeamShareRecipientType"};
-inline constexpr auto token = QLatin1StringView{"OC\\Core\\Sharing\\Recipient\\TokenShareRecipientType"};
-inline constexpr auto user = QLatin1StringView{"OC\\Core\\Sharing\\Recipient\\UserShareRecipientType"};
+inline constexpr auto email = QLatin1StringOrView{"OC\\Core\\Sharing\\Recipient\\EmailShareRecipientType"};
+inline constexpr auto group = QLatin1StringOrView{"OC\\Core\\Sharing\\Recipient\\GroupShareRecipientType"};
+inline constexpr auto team = QLatin1StringOrView{"OC\\Core\\Sharing\\Recipient\\TeamShareRecipientType"};
+inline constexpr auto token = QLatin1StringOrView{"OC\\Core\\Sharing\\Recipient\\TokenShareRecipientType"};
+inline constexpr auto user = QLatin1StringOrView{"OC\\Core\\Sharing\\Recipient\\UserShareRecipientType"};
 
 }
diff --git a/src/gui/sharing/sharingcontroller.cpp b/src/gui/sharing/sharingcontroller.cpp
index cd89540c2a..3b944735f8 100644
--- a/src/gui/sharing/sharingcontroller.cpp
+++ b/src/gui/sharing/sharingcontroller.cpp
@@ -5,6 +5,8 @@
 
 #include "sharingcontroller.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonArray>
 #include <QJsonDocument>
 #include <QJsonObject>
diff --git a/src/gui/sharing/sharingcontroller.h b/src/gui/sharing/sharingcontroller.h
index 077531853f..e141cf0e62 100644
--- a/src/gui/sharing/sharingcontroller.h
+++ b/src/gui/sharing/sharingcontroller.h
@@ -5,12 +5,14 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
+
 #include <QHash>
 #include <QJsonDocument>
 #include <QList>
 #include <QObject>
 #include <QSet>
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 #include <memory>
 #include <vector>
@@ -27,6 +29,9 @@ class SharingController : public QObject
     Q_OBJECT
     QML_ELEMENT
 
+    // The moc-generated qt_metaTypeArray requires the full type definition of
+    // OCC::Account to instantiate the metatype for AccountPtr.
+    Q_MOC_INCLUDE("libsync/account.h")
     Q_PROPERTY(AccountPtr account READ account WRITE setAccount NOTIFY accountChanged)
     Q_PROPERTY(QList<Share *> shares READ shares NOTIFY sharesChanged)
     Q_PROPERTY(bool creatingShare READ creatingShare NOTIFY creatingShareChanged)
diff --git a/src/gui/sharing/unifiedshare.cpp b/src/gui/sharing/unifiedshare.cpp
index 3b9ba115a0..d23cb568fd 100644
--- a/src/gui/sharing/unifiedshare.cpp
+++ b/src/gui/sharing/unifiedshare.cpp
@@ -6,6 +6,8 @@
 
 #include "unifiedshare.h"
 
+#include "common/qtcompat.h"
+
 #include <QJsonArray>
 #include <QJsonDocument>
 #include <QJsonObject>
diff --git a/src/gui/sharing/unifiedshare.h b/src/gui/sharing/unifiedshare.h
index 692cecd959..3b4e597f04 100644
--- a/src/gui/sharing/unifiedshare.h
+++ b/src/gui/sharing/unifiedshare.h
@@ -5,11 +5,13 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
+
 #include <QJsonArray>
 #include <QJsonDocument>
 #include <QObject>
 
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 #include <memory>
 #include <vector>
diff --git a/src/gui/sharing/unifiedsharelistmodel.cpp b/src/gui/sharing/unifiedsharelistmodel.cpp
index a68c464a26..81601f50b6 100644
--- a/src/gui/sharing/unifiedsharelistmodel.cpp
+++ b/src/gui/sharing/unifiedsharelistmodel.cpp
@@ -5,6 +5,8 @@
 
 #include "unifiedsharelistmodel.h"
 
+#include "common/qtcompat.h"
+
 #include <algorithm>
 #include <utility>
 
diff --git a/src/gui/sharing/unifiedsharelistmodel.h b/src/gui/sharing/unifiedsharelistmodel.h
index d38831e584..6fa90d9bfa 100644
--- a/src/gui/sharing/unifiedsharelistmodel.h
+++ b/src/gui/sharing/unifiedsharelistmodel.h
@@ -5,9 +5,11 @@
 
 #pragma once
 
+#include "common/qtcompat.h"
+
 #include <QAbstractListModel>
 #include <QMetaObject>
-#include <QtQmlIntegration>
+#include QmlIntegrationHeader
 
 namespace OCC::Gui::Sharing
 {
diff --git a/src/gui/socketapi/socketapi.cpp b/src/gui/socketapi/socketapi.cpp
index 232925d5f7..64a5038400 100644
--- a/src/gui/socketapi/socketapi.cpp
+++ b/src/gui/socketapi/socketapi.cpp
@@ -17,6 +17,7 @@
 #include "encryptfolderjob.h"
 #include "theme.h"
 #include "common/syncjournalfilerecord.h"
+#include "common/qtcompat.h"
 #include "syncengine.h"
 #include "syncfileitem.h"
 #include "filesystem.h"
diff --git a/src/gui/sslerrordialog.cpp b/src/gui/sslerrordialog.cpp
index dc2cfe6022..9eb7a4ad9a 100644
--- a/src/gui/sslerrordialog.cpp
+++ b/src/gui/sslerrordialog.cpp
@@ -7,6 +7,7 @@
 #include "configfile.h"
 #include "sslerrordialog.h"
 #include "theme.h"
+#include "common/qtcompat.h"
 
 #include <QtGui>
 #include <QtNetwork>
@@ -218,7 +219,7 @@ QString SslErrorDialog::certDiv(QSslCertificate cert) const
 
     msg += QL("<p>");
 
-    if (cert.effectiveDate() < QDateTime(QDate(2016, 1, 1), QTime(), QTimeZone::UTC)) {
+    if (cert.effectiveDate() < QDateTime(QDate(2016, 1, 1), QTime(), QTimeZoneUTC)) {
 	QString sha1sum = Utility::formatFingerprint(cert.digest(QCryptographicHash::Sha1).toHex());
         msg += tr("Fingerprint (SHA1): <tt>%1</tt>").arg(sha1sum) + QL("<br/>");
     }
diff --git a/src/gui/systray.cpp b/src/gui/systray.cpp
index c765b2aeb8..cc0465dbc6 100644
--- a/src/gui/systray.cpp
+++ b/src/gui/systray.cpp
@@ -46,6 +46,8 @@
 #define NOTIFICATIONS_IFACE "org.freedesktop.Notifications"
 #endif
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC {
@@ -132,6 +134,7 @@ void Systray::createTrayEngine()
 
     _trayEngine->addImportPath(QCoreApplication::applicationDirPath() + "/qml");
     _trayEngine->addImportPath("qrc:/qml/theme");
+    _trayEngine->addImportPath(QStringLiteral("qrc:/qt/qml"));
     _trayEngine->addImageProvider("avatars", new ImageProvider);
     _trayEngine->addImageProvider(QLatin1String("svgimage-custom-color"), new OCC::Ui::SvgImageProvider);
     _trayEngine->addImageProvider(QLatin1String("tray-image-provider"), new TrayImageProvider);
@@ -418,11 +421,11 @@ void Systray::showSearchWindow(int userIndex)
         return;
     }
 
-    QQmlComponent searchWindowComponent(trayEngine(), "com.nextcloud.desktopclient.search"_L1, "SearchWindow"_L1);
+    auto *searchWindowComponent = QmlComponentFromUri(trayEngine(), QStringLiteral("com.nextcloud.desktopclient.search"), QStringLiteral("SearchWindow"));
 
-    if (searchWindowComponent.isError()) {
-        qCWarning(lcSystray) << searchWindowComponent.errorString();
-        qCWarning(lcSystray) << searchWindowComponent.errors();
+    if (searchWindowComponent->isError()) {
+        qCWarning(lcSystray) << searchWindowComponent->errorString();
+        qCWarning(lcSystray) << searchWindowComponent->errors();
         return;
     }
 
@@ -434,7 +437,7 @@ void Systray::showSearchWindow(int userIndex)
                     }},
         {"accountId", targetUserId},
     };
-    const auto createdObject = searchWindowComponent.createWithInitialProperties(initialProperties);
+    const auto createdObject = searchWindowComponent->createWithInitialProperties(initialProperties);
     const auto window = qobject_cast<QQuickWindow *>(createdObject);
     if (!window) {
         qCWarning(lcSystray) << "Search window resulted in creation of object that was not a window!";
@@ -884,14 +887,14 @@ void Systray::createUnifiedSharingDialog(const AccountPtr &account, const QStrin
         {"remotePath", remotePath},
     };
 
-    QQmlComponent fileDetailsDialog(trayEngine(), "com.nextcloud.desktopclient.sharing"_L1, "ShareDialog"_L1);
+    auto *fileDetailsDialog = QmlComponentFromUri(trayEngine(), "com.nextcloud.desktopclient.sharing"_L1, "ShareDialog"_L1);
 
-    if (fileDetailsDialog.isError()) {
-        qCWarning(lcSystray) << fileDetailsDialog.errorString();
+    if (fileDetailsDialog->isError()) {
+        qCWarning(lcSystray) << fileDetailsDialog->errorString();
         return;
     }
 
-    const auto createdDialog = fileDetailsDialog.createWithInitialProperties(initialProperties);
+    const auto createdDialog = fileDetailsDialog->createWithInitialProperties(initialProperties);
     const auto dialog = qobject_cast<QQuickWindow*>(createdDialog);
 
     if (!dialog) {
diff --git a/src/gui/tray/AutoSizingMenu.qml b/src/gui/tray/AutoSizingMenu.qml
index 835bcaa1bd..6cc3a09440 100644
--- a/src/gui/tray/AutoSizingMenu.qml
+++ b/src/gui/tray/AutoSizingMenu.qml
@@ -7,7 +7,7 @@ import QtQuick
 import QtQuick.Controls
 
 Menu {
-    popupType: Popup.Window
+    id: autoSizingMenu
 
     width: {
         var result = 0;
@@ -19,4 +19,11 @@ Menu {
         }
         return result + padding * 2;
     }
+
+    Binding {
+        target: autoSizingMenu
+        property: "popupType"
+        value: Popup.Window
+        when: autoSizingMenu.hasOwnProperty("popupType")
+    }
 }
diff --git a/src/gui/tray/asyncimageresponse.cpp b/src/gui/tray/asyncimageresponse.cpp
index 53b9990609..49b3fe904b 100644
--- a/src/gui/tray/asyncimageresponse.cpp
+++ b/src/gui/tray/asyncimageresponse.cpp
@@ -11,6 +11,8 @@
 #include <QPainter>
 #include <QSvgRenderer>
 
+#include "common/qtcompat.h"
+
 #include "accountmanager.h"
 
 using namespace Qt::StringLiterals;
diff --git a/src/gui/tray/usermodel.cpp b/src/gui/tray/usermodel.cpp
index 50a479d1f8..132a7458f2 100644
--- a/src/gui/tray/usermodel.cpp
+++ b/src/gui/tray/usermodel.cpp
@@ -25,6 +25,7 @@
 #include "activity/activitylistmodel.h"
 #include "userstatusconnector.h"
 #include "common/utility.h"
+#include "common/qtcompat.h"
 
 #ifdef BUILD_FILE_PROVIDER_MODULE
 #include "gui/macOS/fileprovider.h"
diff --git a/src/gui/trayaccountpopup_qt.cpp b/src/gui/trayaccountpopup_qt.cpp
index 636533c9ea..6e4a5dba61 100644
--- a/src/gui/trayaccountpopup_qt.cpp
+++ b/src/gui/trayaccountpopup_qt.cpp
@@ -5,6 +5,8 @@
 
 #include "systray.h"
 
+#include "common/qtcompat.h"
+
 #include "accountmanager.h"
 #include "accountstate.h"
 #include "iconjob.h"
diff --git a/src/gui/userinfo.cpp b/src/gui/userinfo.cpp
index bc14986d1f..0271059353 100644
--- a/src/gui/userinfo.cpp
+++ b/src/gui/userinfo.cpp
@@ -10,6 +10,7 @@
 #include "folderman.h"
 #include "creds/abstractcredentials.h"
 #include <theme.h>
+#include "common/qtcompat.h"
 
 #include <QTimer>
 #include <QJsonDocument>
diff --git a/src/gui/wizard/accountwizardcontroller.cpp b/src/gui/wizard/accountwizardcontroller.cpp
index 54a913ac4c..d1a32021a7 100644
--- a/src/gui/wizard/accountwizardcontroller.cpp
+++ b/src/gui/wizard/accountwizardcontroller.cpp
@@ -5,6 +5,8 @@
 
 #include "wizard/accountwizardcontroller.h"
 
+#include "common/qtcompat.h"
+
 #include "account.h"
 #include "accountmanager.h"
 #include "clientproxy.h"
@@ -2042,7 +2044,7 @@ bool AccountWizardController::checkDowngradeAdvised(QNetworkReply *reply) const
         break;
     }
 
-    return !reply->hasRawHeader("Strict-Transport-Security"_L1);
+    return !QNetworkReplyHasRawHeader(reply, "Strict-Transport-Security");
 }
 
 } // namespace OCC
diff --git a/src/gui/wizard/qml/BrowserAuthFooter.qml b/src/gui/wizard/qml/BrowserAuthFooter.qml.in
similarity index 97%
rename from src/gui/wizard/qml/BrowserAuthFooter.qml
rename to src/gui/wizard/qml/BrowserAuthFooter.qml.in
index 1a08d2445e..412cc6123f 100644
--- a/src/gui/wizard/qml/BrowserAuthFooter.qml
+++ b/src/gui/wizard/qml/BrowserAuthFooter.qml.in
@@ -13,7 +13,7 @@ RowLayout {
     required property QtObject controller
 
     spacing: Style.wizardFooterSpacing
-    uniformCellSizes: true
+    @QML_UNIFORM_CELL_SIZES_TRUE@
 
     WizardButton {
         enabled: !root.controller.busy
diff --git a/src/gui/wizard/qml/WizardButton.qml b/src/gui/wizard/qml/WizardButton.qml.in
similarity index 93%
rename from src/gui/wizard/qml/WizardButton.qml
rename to src/gui/wizard/qml/WizardButton.qml.in
index 9f2681b3f2..73183f07ae 100644
--- a/src/gui/wizard/qml/WizardButton.qml
+++ b/src/gui/wizard/qml/WizardButton.qml.in
@@ -5,7 +5,7 @@
 
 import QtQuick
 import QtQuick.Controls.Basic as BasicControls
-import QtQuick.Effects
+@QML_WIZARD_ICON_TINT_IMPORT@
 import QtQuick.Layouts
 import Style
 
@@ -56,15 +56,7 @@ BasicControls.Button {
                 Accessible.ignored: true
             }
 
-            MultiEffect {
-                objectName: "wizardButtonLeadingIconTint"
-                anchors.fill: leadingIconImage
-                visible: root.tintIcon
-                source: leadingIconImage
-                colorization: 1.0
-                colorizationColor: root.iconTintColor
-                Accessible.ignored: true
-            }
+@QML_WIZARD_BUTTON_ICON_TINT_EFFECT@
         }
 
         Text {
diff --git a/src/gui/wizard/qml/WizardMenuItem.qml b/src/gui/wizard/qml/WizardMenuItem.qml.in
similarity index 84%
rename from src/gui/wizard/qml/WizardMenuItem.qml
rename to src/gui/wizard/qml/WizardMenuItem.qml.in
index 4ac379ccbd..ad2a95095f 100644
--- a/src/gui/wizard/qml/WizardMenuItem.qml
+++ b/src/gui/wizard/qml/WizardMenuItem.qml.in
@@ -5,7 +5,7 @@
 
 import QtQuick
 import QtQuick.Controls.Basic as BasicControls
-import QtQuick.Effects
+@QML_WIZARD_ICON_TINT_IMPORT@
 import QtQuick.Layouts
 
 import Style
@@ -42,15 +42,7 @@ BasicControls.MenuItem {
                 Accessible.ignored: true
             }
 
-            MultiEffect {
-                objectName: "wizardMenuItemIconTint"
-                anchors.fill: menuIconImage
-                visible: root.tintIcon
-                source: menuIconImage
-                colorization: 1.0
-                colorizationColor: root.iconTintColor
-                Accessible.ignored: true
-            }
+@QML_WIZARD_MENU_ITEM_ICON_TINT_EFFECT@
 
             Accessible.ignored: true
         }
diff --git a/src/libsync/CMakeLists.txt b/src/libsync/CMakeLists.txt
index 72fe345dfa..f2b3f1ca49 100644
--- a/src/libsync/CMakeLists.txt
+++ b/src/libsync/CMakeLists.txt
@@ -2,8 +2,9 @@
 # SPDX-FileCopyrightText: 2014 ownCloud GmbH
 # SPDX-License-Identifier: GPL-2.0-or-later
 project(libsync)
-find_package(KF6Archive REQUIRED)
+find_package(KF6Archive)
 include(DefinePlatformDefaults)
+include(CheckCXXSourceCompiles)
 
 set(CMAKE_AUTOMOC TRUE)
 
@@ -170,6 +171,8 @@ set(libsync_SRCS
     chronoelapsedtimer.cpp
     hydrationjob.h
     hydrationjob.cpp
+    kfcompat.h
+    kfcompat.cpp
 )
 
 if (WIN32)
@@ -212,13 +215,14 @@ set(creds_HEADERS
     creds/httpcredentials.h
 )
 
-try_compile(HAS_CLOCK_CAST
-        SOURCE_FROM_CONTENT chrono_test.cpp "
+check_cxx_source_compiles("
 #include <chrono>
 int main() {
-    std::chrono::clock_cast<std::chrono::system_clock>;
-}"
-)
+    const auto systemTime = std::chrono::clock_cast<std::chrono::system_clock>(std::chrono::file_clock::now());
+    (void)systemTime;
+    return 0;
+}
+" HAS_CLOCK_CAST)
 message(STATUS "HAS_CLOCK_CAST: ${HAS_CLOCK_CAST}")
 
 if(HAS_CLOCK_CAST)
@@ -256,7 +260,7 @@ target_link_libraries(nextcloudsync
   Qt::Gui
   Qt::Svg
   Qt::Widgets
-  KF6::Archive
+
   Qt::Core5Compat
 )
 
@@ -273,6 +277,11 @@ if(TARGET OpenVFS::OpenVFS)
   )
 endif()
 
+if (KF6Archive_FOUND)
+   target_link_libraries(nextcloudsync PUBLIC  KF6::Archive)
+   add_definitions(-DHAVE_KARCHIVE)
+endif()
+
 target_compile_features(nextcloudsync
     PRIVATE
         cxx_std_17
diff --git a/src/libsync/abstractnetworkjob.cpp b/src/libsync/abstractnetworkjob.cpp
index 85b38f7163..7dcb92bc7b 100644
--- a/src/libsync/abstractnetworkjob.cpp
+++ b/src/libsync/abstractnetworkjob.cpp
@@ -5,6 +5,7 @@
  */
 
 #include "common/asserts.h"
+#include "common/qtcompat.h"
 #include "networkjobs.h"
 #include "account.h"
 #include "owncloudpropagator.h"
@@ -291,9 +292,7 @@ void AbstractNetworkJob::slotFinished()
                         << " origin=" << requestedUrl.host() << ":" << requestedUrl.port()
                         << " target=" << redirectUrl.host() << ":" << redirectUrl.port();
 
-                    auto headers = request.headers();
-                    headers.removeAll(QHttpHeaders::WellKnownHeader::Authorization);
-                    request.setHeaders(headers);
+                    removeAuthorizationHeaders(request);
                     request.setAttribute(AbstractCredentials::DontAddCredentialsAttribute, true);
                 }
 
diff --git a/src/libsync/accessmanager.cpp b/src/libsync/accessmanager.cpp
index 6fb29e0a4f..8475cc8848 100644
--- a/src/libsync/accessmanager.cpp
+++ b/src/libsync/accessmanager.cpp
@@ -18,6 +18,7 @@
 #include "cookiejar.h"
 #include "accessmanager.h"
 #include "common/utility.h"
+#include "common/qtcompat.h"
 #include "httplogger.h"
 
 using namespace Qt::StringLiterals;
@@ -119,7 +120,7 @@ QNetworkReply *AccessManager::createRequest(QNetworkAccessManager::Operation op,
 
     const auto reply = QNetworkAccessManager::createRequest(op, newRequest, outgoingData);
     HttpLogger::logRequest(reply, op, outgoingData);
-    QObject::connect(reply, &QNetworkReply::requestSent, reply, [reply] {
+    QObject::connect(reply, QNetworkReplyRequestSent, reply, [reply] {
         qCInfo(lcAccessManager()) << "Request has been sent"
                                   << reply->url().toString()
                                   << reply->request().rawHeader("X-Request-ID"_ba)
diff --git a/src/libsync/account.cpp b/src/libsync/account.cpp
index 261cf35653..7b9cc30d4a 100644
--- a/src/libsync/account.cpp
+++ b/src/libsync/account.cpp
@@ -24,6 +24,7 @@
 
 #include "common/syncjournaldb.h"
 #include "common/asserts.h"
+#include "common/qtcompat.h"
 #include "clientsideencryption.h"
 #include "ocsuserstatusconnector.h"
 
@@ -1312,7 +1313,8 @@ void Account::listRemoteFolder(QPromise<OCC::PlaceholderCreateInfo> *promise, co
             return;
         }
 
-        promise->emplaceResult(itemFileName, itemFileName.toStdWString(), absoluteItemPathName, newEntry);
+        QPromiseEmplaceResult(promise, OCC::PlaceholderCreateInfo,
+                              itemFileName, itemFileName.toStdWString(), absoluteItemPathName, newEntry);
     });
 
     promise->start();
diff --git a/src/libsync/capabilities.cpp b/src/libsync/capabilities.cpp
index 918ac2d94f..7e4db496d9 100644
--- a/src/libsync/capabilities.cpp
+++ b/src/libsync/capabilities.cpp
@@ -13,6 +13,8 @@
 #include <QVersionNumber>
 #include <QDebug>
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 namespace OCC {
diff --git a/src/libsync/clientsideencryption.cpp b/src/libsync/clientsideencryption.cpp
index 73a10d4eb2..f0cab6c0cb 100644
--- a/src/libsync/clientsideencryption.cpp
+++ b/src/libsync/clientsideencryption.cpp
@@ -14,11 +14,12 @@
 #include "common/utility.h"
 #include "common/constants.h"
 #include <common/checksums.h>
+#include <common/qtcompat.h>
 #include "wordlist.h"
 
 #include <qt6keychain/keychain.h>
 
-#include <KCompressionDevice>
+#include "kfcompat.h"
 
 #include <QDebug>
 #include <QLoggingCategory>
@@ -2746,26 +2747,12 @@ bool EncryptionHelper::dataDecryption(const QByteArray &key, const QByteArray &i
 
 QByteArray EncryptionHelper::gzipThenEncryptData(const QByteArray &key, const QByteArray &inputData, const QByteArray &iv, QByteArray &returnTag)
 {
-    QBuffer gZipBuffer;
-    auto gZipCompressionDevice = KCompressionDevice(&gZipBuffer, false, KCompressionDevice::GZip);
-    if (!gZipCompressionDevice.open(QIODevice::WriteOnly)) {
-        return {};
-    }
-    const auto bytesWritten = gZipCompressionDevice.write(inputData);
-    gZipCompressionDevice.close();
-    if (bytesWritten < 0) {
-        return {};
-    }
-
-    if (!gZipBuffer.open(QIODevice::ReadOnly)) {
-        return {};
-    }
-
     QByteArray outputData;
     returnTag.clear();
-    const auto gZippedAndNotEncrypted = gZipBuffer.readAll();
+
+    const auto gZippedAndNotEncrypted = gZipData(inputData);
+
     EncryptionHelper::dataEncryption(key, iv, gZippedAndNotEncrypted, outputData, returnTag);
-    gZipBuffer.close();
     return outputData;
 }
 
@@ -2777,23 +2764,7 @@ QByteArray EncryptionHelper::decryptThenUnGzipData(const QByteArray &key, const
         return {};
     }
 
-    QBuffer gZipBuffer;
-    if (!gZipBuffer.open(QIODevice::WriteOnly)) {
-        return {};
-    }
-    const auto bytesWritten = gZipBuffer.write(decryptedAndUnGzipped);
-    gZipBuffer.close();
-    if (bytesWritten < 0) {
-        return {};
-    }
-
-    auto gZipUnCompressionDevice = KCompressionDevice(&gZipBuffer, false, KCompressionDevice::GZip);
-    if (!gZipUnCompressionDevice.open(QIODevice::ReadOnly)) {
-        return {};
-    }
-
-    decryptedAndUnGzipped = gZipUnCompressionDevice.readAll();
-    gZipUnCompressionDevice.close();
+    decryptedAndUnGzipped = unGzipData(decryptedAndUnGzipped);
 
     return decryptedAndUnGzipped;
 }
diff --git a/src/libsync/discovery.cpp b/src/libsync/discovery.cpp
index 53dd69b452..418fe55d88 100644
--- a/src/libsync/discovery.cpp
+++ b/src/libsync/discovery.cpp
@@ -23,6 +23,7 @@
 #include <QThreadPool>
 #include <common/checksums.h>
 #include <common/constants.h>
+#include <common/qtcompat.h>
 #include "csync_exclude.h"
 #include "csync.h"
 
diff --git a/src/libsync/discoveryphase.cpp b/src/libsync/discoveryphase.cpp
index 2ad6fb4bbb..d172f87168 100644
--- a/src/libsync/discoveryphase.cpp
+++ b/src/libsync/discoveryphase.cpp
@@ -18,6 +18,7 @@
 #include "common/asserts.h"
 #include "common/checksums.h"
 #include "common/filesystembase.h"
+#include "common/qtcompat.h"
 
 #include <csync_exclude.h>
 #include "vio/csync_vio_local.h"
diff --git a/src/libsync/encryptedfoldermetadatahandler.cpp b/src/libsync/encryptedfoldermetadatahandler.cpp
index 718092f0b8..309e731cbe 100644
--- a/src/libsync/encryptedfoldermetadatahandler.cpp
+++ b/src/libsync/encryptedfoldermetadatahandler.cpp
@@ -10,6 +10,7 @@
 #include "common/syncjournaldb.h"
 #include "clientsideencryptionjobs.h"
 #include "clientsideencryption.h"
+#include "common/qtcompat.h"
 
 #include <QLoggingCategory>
 #include <QNetworkReply>
diff --git a/src/libsync/encryptfolderjob.cpp b/src/libsync/encryptfolderjob.cpp
index ac7b14ec71..f0592a3feb 100644
--- a/src/libsync/encryptfolderjob.cpp
+++ b/src/libsync/encryptfolderjob.cpp
@@ -8,6 +8,7 @@
 #include "common/syncjournaldb.h"
 #include "clientsideencryptionjobs.h"
 #include "foldermetadata.h"
+#include "common/qtcompat.h"
 #include <QLoggingCategory>
 
 using namespace Qt::StringLiterals;
diff --git a/src/libsync/filesystem.cpp b/src/libsync/filesystem.cpp
index 0b1009b9d1..a09b7f4916 100644
--- a/src/libsync/filesystem.cpp
+++ b/src/libsync/filesystem.cpp
@@ -7,6 +7,7 @@
 #include "filesystem.h"
 
 #include "common/utility.h"
+#include "common/qtcompat.h"
 #include "csync.h"
 #include "vio/csync_vio_local.h"
 #include "std/c_time.h"
diff --git a/src/libsync/foldermetadata.cpp b/src/libsync/foldermetadata.cpp
index a4b7c6fdbb..72c734b8bd 100644
--- a/src/libsync/foldermetadata.cpp
+++ b/src/libsync/foldermetadata.cpp
@@ -4,6 +4,7 @@
  */
 
 #include "account.h"
+#include "common/qtcompat.h"
 #include "encryptedfoldermetadatahandler.h"
 #include "foldermetadata.h"
 #include "clientsideencryption.h"
diff --git a/src/libsync/kfcompat.cpp b/src/libsync/kfcompat.cpp
new file mode 100644
index 0000000000..09a0552883
--- /dev/null
+++ b/src/libsync/kfcompat.cpp
@@ -0,0 +1,176 @@
+
+#include "kfcompat.h"
+
+//-----------------------------------------------------------------------------
+
+#include <QByteArray>
+#include <QBuffer>
+
+//-----------------------------------------------------------------------------
+
+#ifdef HAVE_KARCHIVE
+
+//-----------------------------------------------------------------------------
+
+#include <KCompressionDevice>
+
+//-----------------------------------------------------------------------------
+
+QByteArray gZipData(const QByteArray& inputData)
+{
+    QBuffer gZipBuffer;
+    auto gZipCompressionDevice = KCompressionDevice(&gZipBuffer, false, KCompressionDevice::GZip);
+    if (!gZipCompressionDevice.open(QIODevice::WriteOnly)) {
+        return {};
+    }
+    const auto bytesWritten = gZipCompressionDevice.write(inputData);
+    gZipCompressionDevice.close();
+    if (bytesWritten < 0) {
+        return {};
+    }
+
+    if (!gZipBuffer.open(QIODevice::ReadOnly)) {
+        return {};
+    }
+
+    const auto gZipped = gZipBuffer.readAll();
+    gZipBuffer.close();
+    return gZipped;
+}
+
+//-----------------------------------------------------------------------------
+
+QByteArray unGzipData(const QByteArray& inputData)
+{
+    QBuffer gZipBuffer;
+    if (!gZipBuffer.open(QIODevice::WriteOnly)) {
+        return {};
+    }
+    const auto bytesWritten = gZipBuffer.write(inputData);
+    gZipBuffer.close();
+    if (bytesWritten < 0) {
+        return {};
+    }
+
+    auto gZipUnCompressionDevice = KCompressionDevice(&gZipBuffer, false, KCompressionDevice::GZip);
+    if (!gZipUnCompressionDevice.open(QIODevice::ReadOnly)) {
+        return {};
+    }
+
+    auto unGzipped = gZipUnCompressionDevice.readAll();
+    gZipUnCompressionDevice.close();
+    return unGzipped;
+}
+
+
+//-----------------------------------------------------------------------------
+#else // HAVE_KARCHIVE
+//-----------------------------------------------------------------------------
+
+#include <zlib.h>
+#include <sys/mman.h>
+
+//-----------------------------------------------------------------------------
+
+QByteArray gZipData(const QByteArray& inputData)
+{
+    auto fd = memfd_create("gzipThenEncryptData", 0);
+    if (fd < 0) {
+        return {};
+    }
+
+    auto gzFile = gzdopen(fd, "wb");
+    if (gzFile == nullptr) {
+        close(fd);
+        return {};
+    }
+
+    if (gzwrite(gzFile, inputData.data(), inputData.length())!=inputData.length()) {
+        gzclose(gzFile);
+        close(fd);
+        return {};
+    }
+
+    if (gzflush(gzFile, Z_FINISH)!=Z_OK) {
+        close(fd);
+        return {};
+    }
+
+    auto gzippedSize = lseek(fd, 0, SEEK_CUR);
+    if (gzippedSize < 0) {
+        close(fd);
+        return {};
+    }
+
+    if (lseek(fd, 0, SEEK_SET)!=0) {
+        close(fd);
+        return {};
+    }
+
+    QByteArray gZipped(gzippedSize, Qt::Initialization::Uninitialized);
+
+    if (read(fd, gZipped.data(), gzippedSize)!=gzippedSize) {
+        close(fd);
+        return {};
+    }
+
+    close(fd);
+    return gZipped;
+}
+
+//-----------------------------------------------------------------------------
+
+QByteArray unGzipData(const QByteArray& inputData)
+{
+    auto fd = memfd_create("decryptThenUnGzipData", 0);
+    if (fd < 0) {
+        return {};
+    }
+
+    if (write(fd, inputData.data(), inputData.length())!=inputData.length()) {
+        close(fd);
+        return {};
+    }
+
+    auto gzippedSize = lseek(fd, 0, SEEK_CUR);
+    if (gzippedSize < 0) {
+        close(fd);
+        return {};
+    }
+
+    if (lseek(fd, 0, SEEK_SET)!=0) {
+        close(fd);
+        return {};
+    }
+
+    auto gzFile = gzdopen(fd, "rb");
+    if (gzFile == nullptr) {
+        close(fd);
+        return {};
+    }
+
+    QBuffer gunzipBuffer;
+    if (!gunzipBuffer.open(QIODevice::WriteOnly)) {
+        return {};
+    }
+
+    while(!gzeof(gzFile)) {
+        char buf[4096];
+        auto a = gzread(gzFile, buf, sizeof(buf));
+        if (a<0) {
+            gunzipBuffer.close();
+            gzclose(gzFile);
+            return {};
+        }
+
+        gunzipBuffer.write(buf, a);
+    }
+
+    gzclose(gzFile);
+    gunzipBuffer.close();
+
+    return gunzipBuffer.data();
+}
+
+//-----------------------------------------------------------------------------
+#endif // HAVE_KARCHIVE
diff --git a/src/libsync/kfcompat.h b/src/libsync/kfcompat.h
new file mode 100644
index 0000000000..ba7eb44067
--- /dev/null
+++ b/src/libsync/kfcompat.h
@@ -0,0 +1,7 @@
+#pragma once
+
+class QByteArray;
+
+QByteArray gZipData(const QByteArray& inputData);
+
+QByteArray unGzipData(const QByteArray& inputData);
diff --git a/src/libsync/logger.cpp b/src/libsync/logger.cpp
index 19370b0cd4..0074dc26fe 100644
--- a/src/libsync/logger.cpp
+++ b/src/libsync/logger.cpp
@@ -7,6 +7,7 @@
 #include "logger.h"
 
 #include "config.h"
+#include "common/qtcompat.h"
 
 #include <QDir>
 #include <QRegularExpression>
diff --git a/src/libsync/networkjobs.cpp b/src/libsync/networkjobs.cpp
index ef511669a2..8266beedad 100644
--- a/src/libsync/networkjobs.cpp
+++ b/src/libsync/networkjobs.cpp
@@ -10,6 +10,7 @@
 #include "owncloudpropagator.h"
 #include "clientsideencryption.h"
 #include "common/checksums.h"
+#include "common/qtcompat.h"
 
 #include "creds/abstractcredentials.h"
 #include "creds/httpcredentials.h"
@@ -809,7 +810,7 @@ bool PropfindJob::finished()
         // Parse DAV response
         auto domDocument = QDomDocument();
 
-        if (const auto res = domDocument.setContent(reply(), QDomDocument::ParseOption::UseNamespaceProcessing); !res) {
+        if (const auto res = QDomDocumentSetContentsUseNamespaceProcessing(domDocument, reply()); !res) {
             qCWarning(lcPropfindJob) << "XML parser error: " << res.errorMessage << res.errorLine << res.errorColumn;
             Q_EMIT finishedWithError(reply());
 
diff --git a/src/libsync/ocsuserstatusconnector.cpp b/src/libsync/ocsuserstatusconnector.cpp
index c3253ca31d..6bd2415f57 100644
--- a/src/libsync/ocsuserstatusconnector.cpp
+++ b/src/libsync/ocsuserstatusconnector.cpp
@@ -6,6 +6,7 @@
 #include "ocsuserstatusconnector.h"
 #include "account.h"
 #include "userstatusconnector.h"
+#include "common/qtcompat.h"
 
 #include <networkjobs.h>
 
diff --git a/src/libsync/propagateuploadng.cpp b/src/libsync/propagateuploadng.cpp
index 4dcc3e8730..083f2df7ad 100644
--- a/src/libsync/propagateuploadng.cpp
+++ b/src/libsync/propagateuploadng.cpp
@@ -18,6 +18,7 @@
 #include "propagateremotemove.h"
 #include "deletejob.h"
 #include "common/asserts.h"
+#include "common/qtcompat.h"
 
 #include <QNetworkAccessManager>
 #include <QFileInfo>
diff --git a/src/libsync/pushnotifications.cpp b/src/libsync/pushnotifications.cpp
index 4bc20fc37e..0a13d680c4 100644
--- a/src/libsync/pushnotifications.cpp
+++ b/src/libsync/pushnotifications.cpp
@@ -3,6 +3,7 @@
  * SPDX-License-Identifier: GPL-2.0-or-later
  */
 
+#include "common/qtcompat.h"
 #include "pushnotifications.h"
 #include "creds/abstractcredentials.h"
 #include "account.h"
@@ -25,7 +26,7 @@ PushNotifications::PushNotifications(Account *account, QObject *parent)
     , _account(account)
     , _webSocket(new QWebSocket(QString(), QWebSocketProtocol::VersionLatest, this))
 {
-    connect(_webSocket, QOverload<QAbstractSocket::SocketError>::of(&QWebSocket::errorOccurred), this, &PushNotifications::onWebSocketError);
+    connect(_webSocket, QOverload<QAbstractSocket::SocketError>::of(QWebSocketErrorOccurred), this, &PushNotifications::onWebSocketError);
     connect(_webSocket, &QWebSocket::sslErrors, this, &PushNotifications::onWebSocketSslErrors);
     connect(_webSocket, &QWebSocket::connected, this, &PushNotifications::onWebSocketConnected);
     connect(_webSocket, &QWebSocket::disconnected, this, &PushNotifications::onWebSocketDisconnected);
@@ -71,7 +72,7 @@ void PushNotifications::closeWebSocket()
         _reconnectTimer->stop();
     }
 
-    disconnect(_webSocket, QOverload<QAbstractSocket::SocketError>::of(&QWebSocket::errorOccurred), this, &PushNotifications::onWebSocketError);
+    disconnect(_webSocket, QOverload<QAbstractSocket::SocketError>::of(QWebSocketErrorOccurred), this, &PushNotifications::onWebSocketError);
     disconnect(_webSocket, &QWebSocket::sslErrors, this, &PushNotifications::onWebSocketSslErrors);
 
     _webSocket->close();
@@ -171,7 +172,7 @@ void PushNotifications::openWebSocket()
     const auto webSocketUrl = capabilities.pushNotificationsWebSocketUrl();
 
     qCInfo(lcPushNotifications) << "Open connection to websocket on" << webSocketUrl << "for account" << _account->displayName() << _account->url();
-    connect(_webSocket, QOverload<QAbstractSocket::SocketError>::of(&QWebSocket::errorOccurred), this, &PushNotifications::onWebSocketError);
+    connect(_webSocket, QOverload<QAbstractSocket::SocketError>::of(QWebSocketErrorOccurred), this, &PushNotifications::onWebSocketError);
     connect(_webSocket, &QWebSocket::sslErrors, this, &PushNotifications::onWebSocketSslErrors);
     _webSocket->open(webSocketUrl);
 }
@@ -216,7 +217,7 @@ void PushNotifications::handleNotifyFileId(const QString &message)
 
     QList<qint64> fileIds{};
     QJsonParseError parseError;
-    const auto fileIdsJson = message.mid(NOTIFY_FILE_ID_PREFIX.length());
+    const auto fileIdsJson = message.mid(NOTIFY_FILE_ID_PREFIX.size());
     const auto jsonDoc = QJsonDocument::fromJson(fileIdsJson.toUtf8(), &parseError);
 
     if (parseError.error != QJsonParseError::NoError) {
diff --git a/src/libsync/syncengine.cpp b/src/libsync/syncengine.cpp
index 0a7aa38dd1..801111d959 100644
--- a/src/libsync/syncengine.cpp
+++ b/src/libsync/syncengine.cpp
@@ -18,6 +18,7 @@
 #include "deletejob.h"
 #include "propagatedownload.h"
 #include "common/asserts.h"
+#include "common/qtcompat.h"
 #include "configfile.h"
 #include "discovery.h"
 #include "common/vfs.h"
diff --git a/src/libsync/syncfileitem.cpp b/src/libsync/syncfileitem.cpp
index 45b24351ef..3bd3ee24b4 100644
--- a/src/libsync/syncfileitem.cpp
+++ b/src/libsync/syncfileitem.cpp
@@ -8,6 +8,7 @@
 #include "common/checksums.h"
 #include "common/syncjournalfilerecord.h"
 #include "common/utility.h"
+#include "common/qtcompat.h"
 #include "helpers.h"
 #include "filesystem.h"
 
diff --git a/src/libsync/theme.cpp b/src/libsync/theme.cpp
index 57286be498..4f643cdb77 100644
--- a/src/libsync/theme.cpp
+++ b/src/libsync/theme.cpp
@@ -9,6 +9,7 @@
 #include "version.h"
 #include "configfile.h"
 #include "common/vfs.h"
+#include "common/qtcompat.h"
 
 #include <QtCore>
 #ifndef TOKEN_AUTH_ONLY
@@ -933,14 +934,12 @@ QString Theme::versionSwitchOutput() const
 
 double Theme::getColorDarkness(const QColor &color)
 {
-    // account for different sensitivity of the human eye to certain colors
-    const double threshold = 1.0 - (0.299 * color.red() + 0.587 * color.green() + 0.114 * color.blue()) / 255.0;
-    return threshold;
+    return ::getColorDarkness(color);
 }
 
 bool Theme::isDarkColor(const QColor &color)
 {
-    return getColorDarkness(color) > 0.5;
+    return ::isDarkColor(color);
 }
 
 QColor Theme::settingsPanelColor(const QPalette &palette)
@@ -1078,7 +1077,7 @@ QColor Theme::destructiveActionTextColor() const
 void Theme::connectToPaletteSignal() const
 {
     if (const auto ptr = qobject_cast<QGuiApplication*>(qApp)) {
-        connect(ptr->styleHints(), &QStyleHints::colorSchemeChanged, this, &Theme::darkModeChanged, Qt::UniqueConnection);
+        connectQStyleHintsColorSchemeChanged(ptr, this, &Theme::darkModeChanged);
     }
 }
 
@@ -1121,19 +1120,6 @@ QVariantMap Theme::systemPalette() const
 bool Theme::darkMode() const
 {
     connectToPaletteSignal();
-    const auto isDarkFromStyle = [] {
-        switch (qGuiApp->styleHints()->colorScheme())
-        {
-        case Qt::ColorScheme::Dark:
-            return true;
-        case Qt::ColorScheme::Light:
-            return false;
-        case Qt::ColorScheme::Unknown:
-            return Theme::isDarkColor(QGuiApplication::palette().window().color());
-        }
-
-        return false;
-    };
 
 #ifdef Q_OS_WIN
     static const auto darkModeSubkey = QStringLiteral("Software\\Microsoft\\Windows\\CurrentVersion\\Themes\\Personalize");
@@ -1144,7 +1130,8 @@ bool Theme::darkMode() const
         }
     }
 #endif
-    return isDarkFromStyle();
+
+    return isDarkMode();
 }
 
 bool Theme::displayLegacyImportDialog() const
diff --git a/test/qml/search/testsearch.qml b/test/qml/search/testsearch.qml
index 4dae6e38cb..1c902ce37f 100644
--- a/test/qml/search/testsearch.qml
+++ b/test/qml/search/testsearch.qml
@@ -696,8 +696,13 @@ Item {
             const iconTint = findChild(wizardHoverButton, "wizardButtonLeadingIconTint")
             verify(iconTint !== null)
             compare(iconTint.visible, true)
-            compare(iconTint.colorization, 1.0)
-            compare(iconTint.colorizationColor.toString(), Style.wizardPrimaryText.toString())
+            const expectedColor = Style.wizardPrimaryText.toString()
+            if (iconTint.hasOwnProperty("colorization")) {
+                compare(iconTint.colorization, 1.0)
+                compare(iconTint.colorizationColor.toString(), expectedColor)
+            } else {
+                compare(iconTint.color.toString(), expectedColor)
+            }
         }
 
         function test_trailingIconCannotOverlapLongText() {
@@ -737,8 +742,13 @@ Item {
             const iconTint = findChild(wizardMenuHoverItem, "wizardMenuItemIconTint")
             verify(iconTint !== null)
             compare(iconTint.visible, true)
-            compare(iconTint.colorization, 1.0)
-            compare(iconTint.colorizationColor.toString(), Style.wizardPrimaryText.toString())
+            const expectedColor = Style.wizardPrimaryText.toString()
+            if (iconTint.hasOwnProperty("colorization")) {
+                compare(iconTint.colorization, 1.0)
+                compare(iconTint.colorizationColor.toString(), expectedColor)
+            } else {
+                compare(iconTint.color.toString(), expectedColor)
+            }
         }
     }
 
diff --git a/test/syncenginetestutils.cpp b/test/syncenginetestutils.cpp
index b0f774a948..87dc67d765 100644
--- a/test/syncenginetestutils.cpp
+++ b/test/syncenginetestutils.cpp
@@ -440,7 +440,7 @@ FakePropfindReply::FakePropfindReply(FileInfo &remoteRootFileInfo, QNetworkAcces
         xml.writeTextElement(davUri, QStringLiteral("getcontentlength"), QString::number(fileInfo.size));
         xml.writeTextElement(davUri, QStringLiteral("getetag"), QStringLiteral("\"%1\"").arg(QString::fromLatin1(fileInfo.etag)));
         xml.writeTextElement(ocUri, QStringLiteral("quota-available-bytes"), fileInfo.folderQuota.bytesAvailableString());
-        xml.writeTextElement(ocUri, QStringLiteral("quota-used-bytes"), std::to_string(fileInfo.folderQuota.bytesUsed));
+        xml.writeTextElement(ocUri, QStringLiteral("quota-used-bytes"), QString::number(fileInfo.folderQuota.bytesUsed));
         xml.writeTextElement(ocUri, QStringLiteral("permissions"), !fileInfo.permissions.isNull() ? QString(fileInfo.permissions.toString()) : fileInfo.isShared ? QStringLiteral("GSRDNVCKW") : QStringLiteral("GRDNVCKW"));
         if (fileInfo.isShared) {
             if (fileInfo.downloadForbidden) {
diff --git a/test/testaccount.cpp b/test/testaccount.cpp
index c127d38970..e7f24603ee 100644
--- a/test/testaccount.cpp
+++ b/test/testaccount.cpp
@@ -12,6 +12,7 @@
 #include <QTemporaryDir>
 #include <QtTest>
 
+#include "common/qtcompat.h"
 #include "account.h"
 #include "accountstate.h"
 #include "logger.h"
diff --git a/test/testaccountsettings.cpp b/test/testaccountsettings.cpp
index 190a58b0f9..95accbfa30 100644
--- a/test/testaccountsettings.cpp
+++ b/test/testaccountsettings.cpp
@@ -28,6 +28,8 @@
 
 #include "accountsettings.h"
 
+#include "common/qtcompat.h"
+
 using namespace OCC;
 
 class TestAccountSettings : public QObject
diff --git a/test/testactivitydata.cpp b/test/testactivitydata.cpp
index 4bfaf2bb5a..a2c5237690 100644
--- a/test/testactivitydata.cpp
+++ b/test/testactivitydata.cpp
@@ -8,6 +8,7 @@
 #include "accountstate.h"
 #include "syncenginetestutils.h"
 #include "testhelper.h"
+#include "common/qtcompat.h"
 
 #include <QTest>
 #include <QHash>
diff --git a/test/testdatefieldbackend.cpp b/test/testdatefieldbackend.cpp
index a0c21bd2d1..ba603fb759 100644
--- a/test/testdatefieldbackend.cpp
+++ b/test/testdatefieldbackend.cpp
@@ -10,6 +10,7 @@
 #include <QTest>
 #include <QSignalSpy>
 #include <QStandardPaths>
+#include <QTimeZone>
 
 using namespace OCC;
 
diff --git a/test/testfilesystem.cpp b/test/testfilesystem.cpp
index 2df15c6b43..a1348042bf 100644
--- a/test/testfilesystem.cpp
+++ b/test/testfilesystem.cpp
@@ -12,6 +12,7 @@
 #include <QTemporaryFile>
 
 #include "common/filesystembase.h"
+#include "common/qtcompat.h"
 #include "logger.h"
 
 #include "libsync/filesystem.h"
diff --git a/test/testfolder.cpp b/test/testfolder.cpp
index bb0d5c4e6c..6a91b0f7c0 100644
--- a/test/testfolder.cpp
+++ b/test/testfolder.cpp
@@ -12,6 +12,7 @@
 
 #include "accountstate.h"
 #include "common/vfs.h"
+#include "common/qtcompat.h"
 #include "folder.h"
 
 #include "account.h"
diff --git a/test/testfolderman.cpp b/test/testfolderman.cpp
index b3d13af8d5..c8c5d5fedc 100644
--- a/test/testfolderman.cpp
+++ b/test/testfolderman.cpp
@@ -14,6 +14,7 @@
 
 #include "QtTest/qtestcase.h"
 #include "common/utility.h"
+#include "common/qtcompat.h"
 #include "folderman.h"
 #include "account.h"
 #include "accountstate.h"
diff --git a/test/testlocaldiscovery.cpp b/test/testlocaldiscovery.cpp
index 894f72f03f..61be117c0f 100644
--- a/test/testlocaldiscovery.cpp
+++ b/test/testlocaldiscovery.cpp
@@ -18,6 +18,8 @@
 #include <QThreadPool>
 #include <QTemporaryDir>
 
+#include "common/qtcompat.h"
+
 using namespace OCC;
 using namespace Qt::StringLiterals;
 
diff --git a/test/testnextcloudpropagator.cpp b/test/testnextcloudpropagator.cpp
index ed1b30dde1..34414eff7e 100644
--- a/test/testnextcloudpropagator.cpp
+++ b/test/testnextcloudpropagator.cpp
@@ -17,6 +17,8 @@
 #include "owncloudpropagator_p.h"
 #include "syncenginetestutils.h"
 
+#include "common/qtcompat.h"
+
 using namespace Qt::StringLiterals;
 
 #ifdef HAVE_QHTTPSERVER
diff --git a/test/testsharingdialog.cpp b/test/testsharingdialog.cpp
index 48d9340ab3..33d0f68806 100644
--- a/test/testsharingdialog.cpp
+++ b/test/testsharingdialog.cpp
@@ -910,7 +910,9 @@ class TestSharingDialog : public QObject
         const auto effect = avatarObject->findChild<QObject *>(QStringLiteral("recipientAvatarEffect"));
         QVERIFY(effect);
         QVERIFY(effect->property("visible").toBool());
-        QVERIFY(effect->property("maskEnabled").toBool());
+        if (effect->property("maskEnabled").isValid()) {
+            QVERIFY(effect->property("maskEnabled").toBool());
+        }
         const auto mask = avatarObject->findChild<QObject *>(QStringLiteral("recipientAvatarMaskShape"));
         QVERIFY(mask);
         QCOMPARE(effect->property("maskSource").value<QObject *>(), mask);
diff --git a/test/testsyncengine.cpp b/test/testsyncengine.cpp
index ea7360cafc..be0d8dd02d 100644
--- a/test/testsyncengine.cpp
+++ b/test/testsyncengine.cpp
@@ -18,6 +18,7 @@
 #include "propagatorjobs.h"
 #include "syncengine.h"
 #include "syncoptions.h"
+#include "common/qtcompat.h"
 
 #include <QFile>
 #include <QtTest>
diff --git a/test/testsyncjournaldb.cpp b/test/testsyncjournaldb.cpp
index eea8802e03..74b54fddab 100644
--- a/test/testsyncjournaldb.cpp
+++ b/test/testsyncjournaldb.cpp
@@ -14,6 +14,7 @@
 
 #include "common/syncjournaldb.h"
 #include "common/syncjournalfilerecord.h"
+#include "common/qtcompat.h"
 #include "logger.h"
 
 using namespace OCC;
diff --git a/test/testsyncvirtualfiles.cpp b/test/testsyncvirtualfiles.cpp
index ac4b86b634..38340e67e7 100644
--- a/test/testsyncvirtualfiles.cpp
+++ b/test/testsyncvirtualfiles.cpp
@@ -11,6 +11,7 @@
 #include <QtTest>
 #include "syncenginetestutils.h"
 #include "common/vfs.h"
+#include "common/qtcompat.h"
 #include "config.h"
 #include <syncengine.h>
 
